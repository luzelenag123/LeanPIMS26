{"items":
 [{"range":
   {"start": {"line": 1, "column": 1}, "end": {"line": 1, "column": 15}},
   "kind": "Lean.Parser.Module.header",
   "defines": [],
   "code": 6},
  {"range":
   {"start": {"line": 3, "column": 1}, "end": {"line": 3, "column": 25}},
   "kind": "Lean.Parser.Command.namespace",
   "defines": [],
   "code": 12},
  {"range":
   {"start": {"line": 5, "column": 1}, "end": {"line": 5, "column": 15}},
   "kind": "Lean.Parser.Command.universe",
   "defines": [],
   "code": 21},
  {"range":
   {"start": {"line": 38, "column": 1}, "end": {"line": 40, "column": 23}},
   "kind": "Lean.Parser.Command.declaration",
   "defines":
   ["LeanW26.Monads.Maybe.none",
    "LeanW26.Monads.Maybe.some",
    "LeanW26.Monads.Maybe"],
   "code": 52},
  {"range":
   {"start": {"line": 42, "column": 1}, "end": {"line": 42, "column": 11}},
   "kind": "Lean.Parser.Command.open",
   "defines": [],
   "code": 57},
  {"range":
   {"start": {"line": 46, "column": 1}, "end": {"line": 48, "column": 19}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.first"],
   "code": 95},
  {"range":
   {"start": {"line": 50, "column": 1}, "end": {"line": 50, "column": 26}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 109},
  {"range":
   {"start": {"line": 51, "column": 1}, "end": {"line": 51, "column": 28}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 115},
  {"range":
   {"start": {"line": 52, "column": 1}, "end": {"line": 52, "column": 20}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 130},
  {"range":
   {"start": {"line": 60, "column": 1}, "end": {"line": 61, "column": 52}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.first_char"],
   "code": 177},
  {"range":
   {"start": {"line": 63, "column": 1}, "end": {"line": 63, "column": 59}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 188},
  {"range":
   {"start": {"line": 75, "column": 1}, "end": {"line": 77, "column": 20}},
   "kind": "Lean.Parser.Command.declaration",
   "defines":
   ["LeanW26.Monads.Oops.except",
    "LeanW26.Monads.Oops.ok",
    "LeanW26.Monads.Oops"],
   "code": 204},
  {"range":
   {"start": {"line": 79, "column": 1}, "end": {"line": 79, "column": 10}},
   "kind": "Lean.Parser.Command.open",
   "defines": [],
   "code": 209},
  {"range":
   {"start": {"line": 81, "column": 1}, "end": {"line": 83, "column": 17}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.first'"],
   "code": 230},
  {"range":
   {"start": {"line": 85, "column": 1}, "end": {"line": 85, "column": 27}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 239},
  {"range":
   {"start": {"line": 86, "column": 1}, "end": {"line": 86, "column": 29}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 244},
  {"range":
   {"start": {"line": 87, "column": 1}, "end": {"line": 87, "column": 21}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 251},
  {"range":
   {"start": {"line": 98, "column": 1}, "end": {"line": 98, "column": 48}},
   "kind": "Lean.Parser.Command.variable",
   "defines": [],
   "code": 265},
  {"range":
   {"start": {"line": 100, "column": 1}, "end": {"line": 104, "column": 25}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.first_two'"],
   "code": 287},
  {"range":
   {"start": {"line": 113, "column": 1}, "end": {"line": 116, "column": 27}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.andThen"],
   "code": 309},
  {"range":
   {"start": {"line": 120, "column": 1}, "end": {"line": 123, "column": 15}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.first_two"],
   "code": 329},
  {"range":
   {"start": {"line": 125, "column": 1}, "end": {"line": 125, "column": 30}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 336},
  {"range":
   {"start": {"line": 126, "column": 1}, "end": {"line": 126, "column": 21}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 343},
  {"range":
   {"start": {"line": 127, "column": 1}, "end": {"line": 127, "column": 24}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 352},
  {"range":
   {"start": {"line": 128, "column": 1}, "end": {"line": 128, "column": 30}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 361},
  {"range":
   {"start": {"line": 137, "column": 1}, "end": {"line": 137, "column": 29}},
   "kind": "Lean.Parser.Command.mixfix",
   "defines": [],
   "code": 369},
  {"range":
   {"start": {"line": 142, "column": 1}, "end": {"line": 145, "column": 13}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.first_two''"],
   "code": 388},
  {"range":
   {"start": {"line": 181, "column": 1}, "end": {"line": 185, "column": 29}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": [],
   "code": 412},
  {"range":
   {"start": {"line": 194, "column": 1}, "end": {"line": 197, "column": 13}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.first_two_m"],
   "code": 430},
  {"range":
   {"start": {"line": 206, "column": 1}, "end": {"line": 209, "column": 13}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.first_two_do"],
   "code": 451},
  {"range":
   {"start": {"line": 211, "column": 1}, "end": {"line": 211, "column": 24}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 458},
  {"range":
   {"start": {"line": 212, "column": 1}, "end": {"line": 212, "column": 30}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 463},
  {"range":
   {"start": {"line": 249, "column": 1}, "end": {"line": 251, "column": 9}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.doubleM"],
   "code": 483},
  {"range":
   {"start": {"line": 253, "column": 1}, "end": {"line": 253, "column": 23}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 500},
  {"range":
   {"start": {"line": 254, "column": 1}, "end": {"line": 254, "column": 32}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 510},
  {"range":
   {"start": {"line": 255, "column": 1}, "end": {"line": 255, "column": 19}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 521},
  {"range":
   {"start": {"line": 264, "column": 1}, "end": {"line": 271, "column": 40}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.main"],
   "code": 557},
  {"range":
   {"start": {"line": 273, "column": 1}, "end": {"line": 273, "column": 11}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 566},
  {"range":
   {"start": {"line": 289, "column": 1}, "end": {"line": 292, "column": 14}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.pairs"],
   "code": 588},
  {"range":
   {"start": {"line": 294, "column": 1}, "end": {"line": 294, "column": 12}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 618},
  {"range":
   {"start": {"line": 296, "column": 1}, "end": {"line": 300, "column": 15}},
   "kind": "Lean.Parser.Command.declaration",
   "defines": ["LeanW26.Monads.prods"],
   "code": 641},
  {"range":
   {"start": {"line": 302, "column": 1}, "end": {"line": 504, "column": 1}},
   "kind": "Lean.Parser.Command.eval",
   "defines": [],
   "code": 675},
  {"range":
   {"start": {"line": 306, "column": 1}, "end": {"line": 306, "column": 7}},
   "kind": "Lean.Parser.Command.import",
   "defines": [],
   "code": 683}],
 "data":
 {"tokens":
  {"99":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-1019",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "93":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Maybe.some.{u} {α : Type u} : α → Maybe α",
      "name": ["LeanW26", "Monads", "Maybe", "some"],
      "isDef": false,
      "docs": null}},
    "content": "some"},
   "91": {"kind": "unknown", "content": "_"},
   "89": {"kind": "unknown", "content": "::"},
   "87": {"kind": {"var": {"type": "α", "name": "_uniq.1655"}}, "content": "x"},
   "85":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Maybe.none.{u} {α : Type u} : Maybe α",
      "name": ["LeanW26", "Monads", "Maybe", "none"],
      "isDef": false,
      "docs": null}},
    "content": "none"},
   "83": {"kind": "unknown", "content": "=>"},
   "81": {"kind": "unknown", "content": "]"},
   "8":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.namespace-16",
      "name": ["Lean", "Parser", "Command", "namespace"],
      "docs":
      "`namespace <id>` opens a section with label `<id>` that influences naming and name resolution inside\nthe section:\n* Declarations names are prefixed: `def seventeen : ℕ := 17` inside a namespace `Nat` is given the\n  full name `Nat.seventeen`.\n* Names introduced by `export` declarations are also prefixed by the identifier.\n* All names starting with `<id>.` become available in the namespace without the prefix. These names\n  are preferred over names introduced by outer namespaces or `open`.\n* Within a namespace, declarations can be `protected`, which excludes them from the effects of\n  opening the namespace.\n\nAs with `section`, namespaces can be nested and the scope of a namespace is terminated by a\ncorresponding `end <id>` or the end of the file.\n\n`namespace` also acts like `section` in delimiting the scope of `variable`, `open`, and other scoped commands.\n"}},
    "content": "namespace"},
   "79": {"kind": "unknown", "content": "["},
   "77":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-971",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "with"},
   "75":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-971",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "match"},
   "73": {"kind": "unknown", "content": ":="},
   "71":
   {"kind":
    {"const":
     {"signature": "List.{u} (α : Type u) : Type u",
      "name": ["List"],
      "isDef": false,
      "docs":
      "Linked lists: ordered lists, in which each element has a reference to the next element.\n\nMost operations on linked lists take time proportional to the length of the list, because each\nelement must be traversed to find the next element.\n\n`List α` is isomorphic to `Array α`, but they are useful for different things:\n* `List α` is easier for reasoning, and `Array α` is modeled as a wrapper around `List α`.\n* `List α` works well as a persistent data structure, when many copies of the tail are shared. When\n  the value is not shared, `Array α` will have better performance because it can do destructive\n  updates.\n"}},
    "content": "List"},
   "69":
   {"kind": {"var": {"type": "List α", "name": "_uniq.815"}}, "content": "L"},
   "679":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.import-6248",
      "name": ["Lean", "Parser", "Command", "import"],
      "docs": null}},
    "content": "import"},
   "673": {"kind": "unknown", "content": "prods"},
   "67": {"kind": "unknown", "content": "}"},
   "669":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-6233",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "664": {"kind": "unknown", "content": "70"},
   "662": {"kind": "unknown", "content": "60"},
   "660": {"kind": "unknown", "content": "56"},
   "658": {"kind": "unknown", "content": "48"},
   "656": {"kind": "unknown", "content": "42"},
   "654": {"kind": "unknown", "content": "36"},
   "652": {"kind": "unknown", "content": "35"},
   "650": {"kind": "unknown", "content": "30"},
   "65":
   {"kind": {"var": {"type": "Type u", "name": "_uniq.812"}}, "content": "α"},
   "648": {"kind": "unknown", "content": "28"},
   "646": {"kind": "unknown", "content": "24"},
   "644": {"kind": "unknown", "content": "21"},
   "642": {"kind": "unknown", "content": "18"},
   "639": {"kind": {"withType": {"type": "ℕ"}}, "content": "7"},
   "637": {"kind": {"withType": {"type": "ℕ"}}, "content": "6"},
   "635":
   {"kind": {"var": {"type": "ℕ", "name": "_uniq.97174"}}, "content": "c"},
   "633":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLetArrow-6201",
      "name": ["Lean", "Parser", "Term", "doLetArrow"],
      "docs": null}},
    "content": "let"},
   "631":
   {"kind": {"var": {"type": "ℕ", "name": "_uniq.97126"}}, "content": "b"},
   "63": {"kind": "unknown", "content": "{"},
   "629":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLetArrow-6181",
      "name": ["Lean", "Parser", "Term", "doLetArrow"],
      "docs": null}},
    "content": "let"},
   "627":
   {"kind": {"var": {"type": "ℕ", "name": "_uniq.97065"}}, "content": "a"},
   "625":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLetArrow-6162",
      "name": ["Lean", "Parser", "Term", "doLetArrow"],
      "docs": null}},
    "content": "let"},
   "623":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.do-6157",
      "name": ["Lean", "Parser", "Term", "do"],
      "docs": null}},
    "content": "do"},
   "621":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.prods : List ℕ",
      "name": ["LeanW26", "Monads", "prods"],
      "isDef": true,
      "docs": null}},
    "content": "prods"},
   "619":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-6133",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "616":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.pairs : List (ℕ × ℕ)",
      "name": ["LeanW26", "Monads", "pairs"],
      "isDef": false,
      "docs": null}},
    "content": "pairs"},
   "612":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-6120",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "61":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first.{u} {α : Type u} (L : List α) : Maybe α",
      "name": ["LeanW26", "Monads", "first"],
      "isDef": true,
      "docs": null}},
    "content": "first"},
   "607":
   {"kind":
    {"const":
     {"signature":
      "List.cons.{u} {α : Type u} (head : α) (tail : List α) : List α",
      "name": ["List", "cons"],
      "isDef": false,
      "docs":
      "The list whose first element is `head`, where `tail` is the rest of the list.\nUsually written `head :: tail`.\n\n\nConventions for notations in identifiers:\n\n * The recommended spelling of `::` in identifiers is `cons`.\n\n * The recommended spelling of `[a]` in identifiers is `singleton`."}},
    "content": "]"},
   "605": {"kind": "unknown", "content": "5"},
   "603": {"kind": "unknown", "content": "4"},
   "601":
   {"kind":
    {"const":
     {"signature":
      "List.cons.{u} {α : Type u} (head : α) (tail : List α) : List α",
      "name": ["List", "cons"],
      "isDef": false,
      "docs":
      "The list whose first element is `head`, where `tail` is the rest of the list.\nUsually written `head :: tail`.\n\n\nConventions for notations in identifiers:\n\n * The recommended spelling of `::` in identifiers is `cons`.\n\n * The recommended spelling of `[a]` in identifiers is `singleton`."}},
    "content": ","},
   "599":
   {"kind":
    {"const":
     {"signature":
      "Prod.mk.{u, v} {α : Type u} {β : Type v} (fst : α) (snd : β) : α × β",
      "name": ["Prod", "mk"],
      "isDef": false,
      "docs":
      "Constructs a pair. This is usually written `(x, y)` instead of `Prod.mk x y`.\n\n\nConventions for notations in identifiers:\n\n * The recommended spelling of `(a, b)` in identifiers is `mk`."}},
    "content": ")"},
   "597": {"kind": "unknown", "content": "3"},
   "595":
   {"kind":
    {"const":
     {"signature":
      "Prod.mk.{u, v} {α : Type u} {β : Type v} (fst : α) (snd : β) : α × β",
      "name": ["Prod", "mk"],
      "isDef": false,
      "docs":
      "Constructs a pair. This is usually written `(x, y)` instead of `Prod.mk x y`.\n\n\nConventions for notations in identifiers:\n\n * The recommended spelling of `(a, b)` in identifiers is `mk`."}},
    "content": ","},
   "593": {"kind": "unknown", "content": "1"},
   "591":
   {"kind":
    {"const":
     {"signature":
      "Prod.mk.{u, v} {α : Type u} {β : Type v} (fst : α) (snd : β) : α × β",
      "name": ["Prod", "mk"],
      "isDef": false,
      "docs":
      "Constructs a pair. This is usually written `(x, y)` instead of `Prod.mk x y`.\n\n\nConventions for notations in identifiers:\n\n * The recommended spelling of `(a, b)` in identifiers is `mk`."}},
    "content": "("},
   "59":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-919",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "589":
   {"kind":
    {"const":
     {"signature":
      "List.cons.{u} {α : Type u} (head : α) (tail : List α) : List α",
      "name": ["List", "cons"],
      "isDef": false,
      "docs":
      "The list whose first element is `head`, where `tail` is the rest of the list.\nUsually written `head :: tail`.\n\n\nConventions for notations in identifiers:\n\n * The recommended spelling of `::` in identifiers is `cons`.\n\n * The recommended spelling of `[a]` in identifiers is `singleton`."}},
    "content": "["},
   "586": {"kind": {"withType": {"type": "ℕ"}}, "content": "5"},
   "584": {"kind": {"withType": {"type": "ℕ"}}, "content": "4"},
   "582":
   {"kind": {"var": {"type": "ℕ", "name": "_uniq.92810"}}, "content": "b"},
   "580":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLetArrow-6087",
      "name": ["Lean", "Parser", "Term", "doLetArrow"],
      "docs": null}},
    "content": "let"},
   "578":
   {"kind": {"var": {"type": "ℕ", "name": "_uniq.92749"}}, "content": "a"},
   "576":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLetArrow-6068",
      "name": ["Lean", "Parser", "Term", "doLetArrow"],
      "docs": null}},
    "content": "let"},
   "574":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.do-6063",
      "name": ["Lean", "Parser", "Term", "do"],
      "docs": null}},
    "content": "do"},
   "572": {"kind": "unknown", "content": "×"},
   "570":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.pairs : List (ℕ × ℕ)",
      "name": ["LeanW26", "Monads", "pairs"],
      "isDef": true,
      "docs": null}},
    "content": "pairs"},
   "568":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-6030",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "564":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.main : IO Unit",
      "name": ["LeanW26", "Monads", "main"],
      "isDef": false,
      "docs": null}},
    "content": "main"},
   "560":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-5775",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "555": {"kind": "unknown", "content": "}\""},
   "553": {"kind": "unknown", "content": "\"Current directory: {"},
   "551":
   {"kind":
    {"keyword":
     {"occurrence": "termS!_-5747", "name": ["termS!_"], "docs": null}},
    "content": "s!"},
   "55": {"kind": "unknown", "content": "Maybe"},
   "549":
   {"kind":
    {"const":
     {"signature": "IO.currentDir : IO System.FilePath",
      "name": ["IO", "currentDir"],
      "isDef": false,
      "docs":
      "Returns the current working directory of the executing process.\n"}},
    "content": "IO.currentDir"},
   "547":
   {"kind": {"var": {"type": "System.FilePath", "name": "_uniq.90944"}},
    "content": "d"},
   "545":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLetArrow-5710",
      "name": ["Lean", "Parser", "Term", "doLetArrow"],
      "docs": null}},
    "content": "let"},
   "543":
   {"kind": {"str": {"string": "You can examime the filesystem too."}},
    "content": "\"You can examime the filesystem too.\""},
   "541":
   {"kind": {"str": {"string": "you can use IO.getLine"}},
    "content": "\"you can use IO.getLine\""},
   "539":
   {"kind": {"str": {"string": "If you run this code from the command line"}},
    "content": "\"If you run this code from the command line\""},
   "537":
   {"kind": {"str": {"string": "This is the IO Monad"}},
    "content": "\"This is the IO Monad\""},
   "535": {"kind": {"str": {"string": "Hello!"}}, "content": "\"Hello!\""},
   "533":
   {"kind":
    {"const":
     {"signature":
      "IO.println.{u_1} {α : Type u_1} [ToString α] (s : α) : IO Unit",
      "name": ["IO", "println"],
      "isDef": false,
      "docs":
      "Converts `s` to a string using its `ToString α` instance, and prints it with a trailing newline to\nthe current standard output (as determined by `IO.getStdout`).\n"}},
    "content": "IO.println"},
   "531":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.do-5500",
      "name": ["Lean", "Parser", "Term", "do"],
      "docs": null}},
    "content": "do"},
   "53":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.open-835",
      "name": ["Lean", "Parser", "Command", "open"],
      "docs":
      "Makes names from other namespaces visible without writing the namespace prefix.\n\nNames that are made available with `open` are visible within the current `section` or `namespace`\nblock. This makes referring to (type) definitions and theorems easier, but note that it can also\nmake [scoped instances], notations, and attributes from a different namespace available.\n\nThe `open` command can be used in a few different ways:\n\n* `open Some.Namespace.Path1 Some.Namespace.Path2` makes all non-protected names in\n  `Some.Namespace.Path1` and `Some.Namespace.Path2` available without the prefix, so that\n  `Some.Namespace.Path1.x` and `Some.Namespace.Path2.y` can be referred to by writing only `x` and\n  `y`.\n\n* `open Some.Namespace.Path hiding def1 def2` opens all non-protected names in `Some.Namespace.Path`\n  except `def1` and `def2`.\n\n* `open Some.Namespace.Path (def1 def2)` only makes `Some.Namespace.Path.def1` and\n  `Some.Namespace.Path.def2` available without the full prefix, so `Some.Namespace.Path.def3` would\n  be unaffected.\n\n  This works even if `def1` and `def2` are `protected`.\n\n* `open Some.Namespace.Path renaming def1 → def1', def2 → def2'` same as `open Some.Namespace.Path\n  (def1 def2)` but `def1`/`def2`'s names are changed to `def1'`/`def2'`.\n\n  This works even if `def1` and `def2` are `protected`.\n\n* `open scoped Some.Namespace.Path1 Some.Namespace.Path2` **only** opens [scoped instances],\n  notations, and attributes from `Namespace1` and `Namespace2`; it does **not** make any other name\n  available.\n\n* `open <any of the open shapes above> in` makes the names `open`-ed visible only in the next\n  command or expression.\n\n[scoped instance]: https://lean-lang.org/theorem_proving_in_lean4/type_classes.html#scoped-instances\n(Scoped instances in Theorem Proving in Lean)\n\n\n## Examples\n\n```lean\n/-- SKI combinators https://en.wikipedia.org/wiki/SKI_combinator_calculus -/\nnamespace Combinator.Calculus\n  def I (a : α) : α := a\n  def K (a : α) : β → α := fun _ => a\n  def S (x : α → β → γ) (y : α → β) (z : α) : γ := x z (y z)\nend Combinator.Calculus\n\nsection\n  -- open everything under `Combinator.Calculus`, *i.e.* `I`, `K` and `S`,\n  -- until the section ends\n  open Combinator.Calculus\n\n  theorem SKx_eq_K : S K x = I := rfl\nend\n\n-- open everything under `Combinator.Calculus` only for the next command (the next `theorem`, here)\nopen Combinator.Calculus in\ntheorem SKx_eq_K' : S K x = I := rfl\n\nsection\n  -- open only `S` and `K` under `Combinator.Calculus`\n  open Combinator.Calculus (S K)\n\n  theorem SKxy_eq_y : S K x y = y := rfl\n\n  -- `I` is not in scope, we have to use its full path\n  theorem SKxy_eq_Iy : S K x y = Combinator.Calculus.I y := rfl\nend\n\nsection\n  open Combinator.Calculus\n    renaming\n      I → identity,\n      K → konstant\n\n  #check identity\n  #check konstant\nend\n\nsection\n  open Combinator.Calculus\n    hiding S\n\n  #check I\n  #check K\nend\n\nsection\n  namespace Demo\n    inductive MyType\n    | val\n\n    namespace N1\n      scoped infix:68 \" ≋ \" => BEq.beq\n\n      scoped instance : BEq MyType where\n        beq _ _ := true\n\n      def Alias := MyType\n    end N1\n  end Demo\n\n  -- bring `≋` and the instance in scope, but not `Alias`\n  open scoped Demo.N1\n\n  #check Demo.MyType.val == Demo.MyType.val\n  #check Demo.MyType.val ≋ Demo.MyType.val\n  -- #check Alias -- unknown identifier 'Alias'\nend\n```\n"}},
    "content": "open"},
   "529":
   {"kind":
    {"const":
     {"signature": "Unit : Type",
      "name": ["Unit"],
      "isDef": false,
      "docs":
      "The canonical type with one element. This element is written `()`.\n\n`Unit` has a number of uses:\n* It can be used to model control flow that returns from a function call without providing other\n  information.\n* Monadic actions that return `Unit` have side effects without computing values.\n* In polymorphic types, it can be used to indicate that no data is to be stored in a particular\n  field.\n"}},
    "content": "Unit"},
   "527":
   {"kind":
    {"const":
     {"signature": "IO : Type → Type",
      "name": ["IO"],
      "isDef": false,
      "docs":
      "A monad that supports arbitrary side effects and throwing exceptions of type `IO.Error`.\n"}},
    "content": "IO"},
   "525":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.main : IO Unit",
      "name": ["LeanW26", "Monads", "main"],
      "isDef": true,
      "docs": null}},
    "content": "main"},
   "523":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-5478",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "518":
   {"kind":
    {"const":
     {"signature": "Id.{u} (type : Type u) : Type u",
      "name": ["Id"],
      "isDef": false,
      "docs":
      "The identity function on types, used primarily for its `Monad` instance.\n\nThe identity monad is useful together with monad transformers to construct monads for particular\npurposes. Additionally, it can be used with `do`-notation in order to use control structures such as\nlocal mutability, `for`-loops, and early returns in code that does not otherwise use monads.\n\nExamples:\n```lean example\ndef containsFive (xs : List Nat) : Bool := Id.run do\n  for x in xs do\n    if x == 5 then return true\n  return false\n```\n\n```lean example\n#eval containsFive [1, 3, 5, 7]\n```\n```output\ntrue\n```\n"}},
    "content": "Id"},
   "514":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-5356",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "507":
   {"kind":
    {"const":
     {"signature": "Except.{u, v} (ε : Type u) (α : Type v) : Type (max u v)",
      "name": ["Except"],
      "isDef": false,
      "docs":
      "`Except ε α` is a type which represents either an error of type `ε` or a successful result with a\nvalue of type `α`.\n\n`Except ε : Type u → Type v` is a `Monad` that represents computations that may throw exceptions:\nthe `pure` operation is `Except.ok` and the `bind` operation returns the first encountered\n`Except.error`.\n"}},
    "content": "Except"},
   "503":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-5311",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "50": {"kind": "unknown", "content": "→"},
   "497":
   {"kind":
    {"const":
     {"signature": "Option.{u} (α : Type u) : Type u",
      "name": ["Option"],
      "isDef": false,
      "docs":
      "Optional values, which are either `some` around a value from the underlying type or `none`.\n\n`Option` can represent nullable types or computations that might fail. In the codomain of a function\ntype, it can also represent partiality.\n"}},
    "content": "Option"},
   "495":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.doubleM (m : Type → Type) [Monad m] (x : ℕ) : m ℕ",
      "name": ["LeanW26", "Monads", "doubleM"],
      "isDef": false,
      "docs": null}},
    "content": "doubleM"},
   "491":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-5264",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "486": {"kind": "unknown", "content": "2"},
   "484":
   {"kind":
    {"const":
     {"signature": "Option.some.{u} {α : Type u} (val : α) : Option α",
      "name": ["Option", "some"],
      "isDef": false,
      "docs": "Some value of type `α`. "}},
    "content": "Option.some"},
   "481":
   {"kind":
    {"const":
     {"signature":
      "Pure.pure.{u, v} {f : Type u → Type v} [self : Pure f] {α : Type u} : α → f α",
      "name": ["Pure", "pure"],
      "isDef": false,
      "docs":
      "Given `a : α`, then `pure a : f α` represents an action that does nothing and returns `a`.\n\nExamples:\n* `(pure \"hello\" : Option String) = some \"hello\"`\n* `(pure \"hello\" : Except (Array String) String) = Except.ok \"hello\"`\n* `(pure \"hello\" : StateM Nat String).run 105 = (\"hello\", 105)`\n"}},
    "content": "pure"},
   "48":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Maybe.some.{u} {α : Type u} : α → Maybe α",
      "name": ["LeanW26", "Monads", "Maybe", "some"],
      "isDef": true,
      "docs": null}},
    "content": "some"},
   "479": {"kind": "unknown", "content": "*"},
   "477":
   {"kind": {"var": {"type": "ℕ", "name": "_uniq.79637"}}, "content": "y"},
   "475":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLet-5239",
      "name": ["Lean", "Parser", "Term", "doLet"],
      "docs": null}},
    "content": "let"},
   "473":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.do-5234",
      "name": ["Lean", "Parser", "Term", "do"],
      "docs": null}},
    "content": "do"},
   "471":
   {"kind": {"var": {"type": "ℕ", "name": "_uniq.79548"}}, "content": "x"},
   "469":
   {"kind": {"var": {"type": "Type → Type", "name": "_uniq.79539"}},
    "content": "m"},
   "467":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.doubleM (m : Type → Type) [Monad m] (x : ℕ) : m ℕ",
      "name": ["LeanW26", "Monads", "doubleM"],
      "isDef": true,
      "docs": null}},
    "content": "doubleM"},
   "465":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-5171",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "46":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Maybe.{u} (α : Type u) : Type u",
      "name": ["LeanW26", "Monads", "Maybe"],
      "isDef": false,
      "docs": null}},
    "content": "Maybe"},
   "459":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-4614",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "456":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first_two_do.{v} {α : Type v} (L : List α) : Maybe (List α)",
      "name": ["LeanW26", "Monads", "first_two_do"],
      "isDef": false,
      "docs": null}},
    "content": "first_two_do"},
   "452":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-4590",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "449":
   {"kind": {"var": {"type": "α", "name": "_uniq.69840"}}, "content": "y"},
   "447":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLetArrow-4553",
      "name": ["Lean", "Parser", "Term", "doLetArrow"],
      "docs": null}},
    "content": "let"},
   "444": {"kind": "unknown", "content": "←"},
   "442":
   {"kind": {"var": {"type": "α", "name": "_uniq.69814"}}, "content": "x"},
   "440":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.doLetArrow-4486",
      "name": ["Lean", "Parser", "Term", "doLetArrow"],
      "docs": null}},
    "content": "let"},
   "44":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Maybe.none.{u} {α : Type u} : Maybe α",
      "name": ["LeanW26", "Monads", "Maybe", "none"],
      "isDef": true,
      "docs": null}},
    "content": "none"},
   "438":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.do-4481",
      "name": ["Lean", "Parser", "Term", "do"],
      "docs": null}},
    "content": "do"},
   "436":
   {"kind": {"var": {"type": "List α", "name": "_uniq.69773"}}, "content": "L"},
   "434":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first_two_do.{v} {α : Type v} (L : List α) : Maybe (List α)",
      "name": ["LeanW26", "Monads", "first_two_do"],
      "isDef": true,
      "docs": null}},
    "content": "first_two_do"},
   "432":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-4429",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "428":
   {"kind": {"var": {"type": "α", "name": "_uniq.68724"}}, "content": "y"},
   "426":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.fun-4311",
      "name": ["Lean", "Parser", "Term", "fun"],
      "docs": null}},
    "content": "fun"},
   "424":
   {"kind": {"var": {"type": "α", "name": "_uniq.68700"}}, "content": "x"},
   "422":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.fun-4283",
      "name": ["Lean", "Parser", "Term", "fun"],
      "docs": null}},
    "content": "fun"},
   "420": {"kind": "unknown", "content": ">>="},
   "42": {"kind": "unknown", "content": "|"},
   "418":
   {"kind": {"var": {"type": "List α", "name": "_uniq.68675"}}, "content": "L"},
   "416":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first_two_m.{v} {α : Type v} (L : List α) : Maybe (List α)",
      "name": ["LeanW26", "Monads", "first_two_m"],
      "isDef": true,
      "docs": null}},
    "content": "first_two_m"},
   "414":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-4213",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "410":
   {"kind": {"var": {"type": "α✝", "name": "_uniq.67441"}}, "content": "y"},
   "408":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-4029",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "with"},
   "406":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-4029",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "match"},
   "404":
   {"kind": {"var": {"type": "α✝ → Maybe β✝", "name": "_uniq.66627"}},
    "content": "next"},
   "402":
   {"kind": {"var": {"type": "Maybe α✝", "name": "_uniq.66624"}},
    "content": "m"},
   "400":
   {"kind":
    {"const":
     {"signature": "{α β : Type u_1} → Maybe α → (α → Maybe β) → Maybe β",
      "name": ["Bind", "bind"],
      "isDef": false,
      "docs":
      "Sequences two computations, allowing the second to depend on the value computed by the first.\n\nIf `x : m α` and `f : α → m β`, then `x >>= f : m β` represents the result of executing `x` to get\na value of type `α` and then passing it to `f`.\n\n\nConventions for notations in identifiers:\n\n * The recommended spelling of `>>=` in identifiers is `bind`."}},
    "content": "bind"},
   "398":
   {"kind": {"var": {"type": "α✝", "name": "_uniq.65653"}}, "content": "x"},
   "396":
   {"kind":
    {"const":
     {"signature": "{α : Type u_1} → α → Maybe α",
      "name": ["Pure", "pure"],
      "isDef": false,
      "docs":
      "Given `a : α`, then `pure a : f α` represents an action that does nothing and returns `a`.\n\nExamples:\n* `(pure \"hello\" : Option String) = some \"hello\"`\n* `(pure \"hello\" : Except (Array String) String) = Except.ok \"hello\"`\n* `(pure \"hello\" : StateM Nat String).run 105 = (\"hello\", 105)`\n"}},
    "content": "pure"},
   "394":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.whereStructInst-3987",
      "name": ["Lean", "Parser", "Command", "whereStructInst"],
      "docs": null}},
    "content": "where"},
   "392":
   {"kind":
    {"const":
     {"signature": "Monad.{u, v} (m : Type u → Type v) : Type (max (u + 1) v)",
      "name": ["Monad"],
      "isDef": false,
      "docs":
      "[Monads](https://en.wikipedia.org/wiki/Monad_(functional_programming)) are an abstraction of\nsequential control flow and side effects used in functional programming. Monads allow both\nsequencing of effects and data-dependent effects: the values that result from an early step may\ninfluence the effects carried out in a later step.\n\nThe `Monad` API may be used directly. However, it is most commonly accessed through\n[`do`-notation](https://lean-lang.org/doc/reference/4.28.0-rc1/find/?domain=Verso.Genre.Manual.section&name=do-notation).\n\nMost `Monad` instances provide implementations of `pure` and `bind`, and use default implementations\nfor the other methods inherited from `Applicative`. Monads should satisfy certain laws, but\ninstances are not required to prove this. An instance of `LawfulMonad` expresses that a given\nmonad's operations are lawful.\n"}},
    "content": "Monad"},
   "390":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.instance-3964",
      "name": ["Lean", "Parser", "Command", "instance"],
      "docs": null}},
    "content": "instance"},
   "39":
   {"kind":
    {"keyword": {"occurrence": "null-781", "name": ["null"], "docs": null}},
    "content": "where"},
   "386":
   {"kind": {"var": {"type": "α", "name": "_uniq.64518"}}, "content": "y"},
   "384":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.fun-3040",
      "name": ["Lean", "Parser", "Term", "fun"],
      "docs": null}},
    "content": "fun"},
   "382":
   {"kind": {"var": {"type": "α", "name": "_uniq.64503"}}, "content": "x"},
   "380":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.fun-3012",
      "name": ["Lean", "Parser", "Term", "fun"],
      "docs": null}},
    "content": "fun"},
   "378": {"kind": "unknown", "content": "~~>"},
   "375":
   {"kind": {"var": {"type": "List α", "name": "_uniq.64487"}}, "content": "L"},
   "373":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first_two''.{v} {α : Type v} (L : List α) : Maybe (List α)",
      "name": ["LeanW26", "Monads", "first_two''"],
      "isDef": true,
      "docs": null}},
    "content": "first_two''"},
   "371":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-2942",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "37": {"kind": "unknown", "content": ")"},
   "367": {"kind": {"str": {"string": " ~~> "}}, "content": "\" ~~> \""},
   "365": {"kind": "unknown", "content": "55"},
   "363":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.infixl-2843",
      "name": ["Lean", "Parser", "Command", "infixl"],
      "docs":
      "`infixl:prec \"op\" => f` is equivalent to `notation:prec x:prec \"op\" y:prec1 => f x y`, where `prec1 := prec + 1`.\n"}},
    "content": "infixl"},
   "359": {"kind": {"withType": {"type": "ℕ"}}, "content": "40"},
   "357": {"kind": {"withType": {"type": "ℕ"}}, "content": "30"},
   "353":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-2706",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "350": {"kind": {"withType": {"type": "ℕ"}}, "content": "20"},
   "35": {"kind": {"levelVar": {"name": ["u"]}}, "content": "u"},
   "346":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-2682",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "341": {"kind": {"withType": {"type": "ℕ"}}, "content": "10"},
   "337":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-2661",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "334":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first_two.{v} {α : Type v} (L : List α) : Maybe (List α)",
      "name": ["LeanW26", "Monads", "first_two"],
      "isDef": false,
      "docs": null}},
    "content": "first_two"},
   "330":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-2631",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "33":
   {"kind":
    {"sort":
     {"doc?": "A type universe. `Type ≡ Type 0`, `Type u ≡ Sort (u + 1)`. "}},
    "content": "Type"},
   "327":
   {"kind": {"var": {"type": "α", "name": "_uniq.40125"}}, "content": "y"},
   "325":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.fun-2606",
      "name": ["Lean", "Parser", "Term", "fun"],
      "docs": null}},
    "content": "fun"},
   "323":
   {"kind":
    {"const":
     {"signature": "List.tail.{u} {α : Type u} : List α → List α",
      "name": ["List", "tail"],
      "isDef": false,
      "docs":
      "Drops the first element of a nonempty list, returning the tail. Returns `[]` when the argument is\nempty.\n\nExamples:\n * `[\"apple\", \"banana\", \"grape\"].tail = [\"banana\", \"grape\"]`\n * `[\"apple\"].tail = []`\n * `([] : List String).tail = []`\n"}},
    "content": "tail"},
   "321":
   {"kind": {"var": {"type": "α", "name": "_uniq.40110"}}, "content": "x"},
   "319":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.fun-2571",
      "name": ["Lean", "Parser", "Term", "fun"],
      "docs": null}},
    "content": "fun"},
   "317":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.andThen.{v, w} {α : Type v} {β : Type w} (maybe : Maybe α) (next : α → Maybe β) : Maybe β",
      "name": ["LeanW26", "Monads", "andThen"],
      "isDef": false,
      "docs": null}},
    "content": "andThen"},
   "315":
   {"kind": {"var": {"type": "List α", "name": "_uniq.40094"}}, "content": "L"},
   "313":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first_two.{v} {α : Type v} (L : List α) : Maybe (List α)",
      "name": ["LeanW26", "Monads", "first_two"],
      "isDef": true,
      "docs": null}},
    "content": "first_two"},
   "311":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-2501",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "31": {"kind": "unknown", "content": ":"},
   "306":
   {"kind": {"var": {"type": "α", "name": "_uniq.39923"}}, "content": "x"},
   "304":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Maybe.some.{u} {α : Type u} : α → Maybe α",
      "name": ["LeanW26", "Monads", "Maybe", "some"],
      "isDef": false,
      "docs": null}},
    "content": "Maybe.some"},
   "301":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Maybe.none.{u} {α : Type u} : Maybe α",
      "name": ["LeanW26", "Monads", "Maybe", "none"],
      "isDef": false,
      "docs": null}},
    "content": "Maybe.none"},
   "3": {"kind": {"moduleName": {"name": ["Mathlib"]}}, "content": "Mathlib"},
   "299":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-2369",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "with"},
   "297":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-2369",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "match"},
   "295":
   {"kind": {"var": {"type": "α → Maybe β", "name": "_uniq.39089"}},
    "content": "next"},
   "293":
   {"kind": {"var": {"type": "Maybe α", "name": "_uniq.39084"}},
    "content": "maybe"},
   "291":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.andThen.{v, w} {α : Type v} {β : Type w} (maybe : Maybe α) (next : α → Maybe β) : Maybe β",
      "name": ["LeanW26", "Monads", "andThen"],
      "isDef": true,
      "docs": null}},
    "content": "andThen"},
   "29":
   {"kind": {"var": {"type": "Type u", "name": "_uniq.2"}}, "content": "α"},
   "289":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-2297",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "285":
   {"kind": {"var": {"type": "α", "name": "_uniq.37948"}}, "content": "y"},
   "282":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-2094",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "with"},
   "280":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-2094",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "match"},
   "278":
   {"kind": {"var": {"type": "List α", "name": "_uniq.37081"}}, "content": "M"},
   "276":
   {"kind": {"var": {"type": "α", "name": "_uniq.37080"}}, "content": "x"},
   "274":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-2054",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "with"},
   "272":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-2054",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "match"},
   "270":
   {"kind": {"var": {"type": "List α", "name": "_uniq.36193"}}, "content": "L"},
   "27": {"kind": "unknown", "content": "("},
   "268":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first_two'.{v} {α : Type v} (L : List α) : Maybe (List α)",
      "name": ["LeanW26", "Monads", "first_two'"],
      "isDef": true,
      "docs": null}},
    "content": "first_two'"},
   "266":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-2004",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "263": {"kind": {"levelVar": {"name": ["w"]}}, "content": "w"},
   "261":
   {"kind": {"var": {"type": "Type w", "name": "_uniq.92681"}}, "content": "β"},
   "259": {"kind": {"levelVar": {"name": ["v"]}}, "content": "v"},
   "257":
   {"kind": {"var": {"type": "Type v", "name": "_uniq.92679"}}, "content": "α"},
   "255":
   {"kind": {"var": {"type": "Type u", "name": "_uniq.92677"}}, "content": "σ"},
   "253":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.variable-1952",
      "name": ["Lean", "Parser", "Command", "variable"],
      "docs":
      "Declares one or more typed variables, or modifies whether already-declared variables are\n  implicit.\n\nIntroduces variables that can be used in definitions within the same `namespace` or `section` block.\nWhen a definition mentions a variable, Lean will add it as an argument of the definition. This is\nuseful in particular when writing many definitions that have parameters in common (see below for an\nexample).\n\nVariable declarations have the same flexibility as regular function parameters. In particular they\ncan be [explicit, implicit][binder docs], or [instance implicit][tpil classes] (in which case they\ncan be anonymous). This can be changed, for instance one can turn explicit variable `x` into an\nimplicit one with `variable {x}`. Note that currently, you should avoid changing how variables are\nbound and declare new variables at the same time; see [issue 2789] for more on this topic.\n\nIn *theorem bodies* (i.e. proofs), variables are not included based on usage in order to ensure that\nchanges to the proof cannot change the statement of the overall theorem. Instead, variables are only\navailable to the proof if they have been mentioned in the theorem header or in an `include` command\nor are instance implicit and depend only on such variables.\n\nSee [*Variables and Sections* from Theorem Proving in Lean][tpil vars] for a more detailed\ndiscussion.\n\n[tpil vars]:\nhttps://lean-lang.org/theorem_proving_in_lean4/dependent_type_theory.html#variables-and-sections\n(Variables and Sections on Theorem Proving in Lean) [tpil classes]:\nhttps://lean-lang.org/theorem_proving_in_lean4/type_classes.html (Type classes on Theorem Proving in\nLean) [binder docs]:\nhttps://leanprover-community.github.io/mathlib4_docs/Lean/Expr.html#Lean.BinderInfo (Documentation\nfor the BinderInfo type) [issue 2789]: https://github.com/leanprover/lean4/issues/2789 (Issue 2789\non github)\n\n## Examples\n\n```lean\nsection\n  variable\n    {α : Type u}      -- implicit\n    (a : α)           -- explicit\n    [instBEq : BEq α] -- instance implicit, named\n    [Hashable α]      -- instance implicit, anonymous\n\n  def isEqual (b : α) : Bool :=\n    a == b\n\n  #check isEqual\n  -- isEqual.{u} {α : Type u} (a : α) [instBEq : BEq α] (b : α) : Bool\n\n  variable\n    {a} -- `a` is implicit now\n\n  def eqComm {b : α} := a == b ↔ b == a\n\n  #check eqComm\n  -- eqComm.{u} {α : Type u} {a : α} [instBEq : BEq α] {b : α} : Prop\nend\n```\n\nThe following shows a typical use of `variable` to factor out definition arguments:\n\n```lean\nvariable (Src : Type)\n\nstructure Logger where\n  trace : List (Src × String)\n#check Logger\n-- Logger (Src : Type) : Type\n\nnamespace Logger\n  -- switch `Src : Type` to be implicit until the `end Logger`\n  variable {Src}\n\n  def empty : Logger Src where\n    trace := []\n  #check empty\n  -- Logger.empty {Src : Type} : Logger Src\n\n  variable (log : Logger Src)\n\n  def len :=\n    log.trace.length\n  #check len\n  -- Logger.len {Src : Type} (log : Logger Src) : Nat\n\n  variable (src : Src) [BEq Src]\n\n  -- at this point all of `log`, `src`, `Src` and the `BEq` instance can all become arguments\n\n  def filterSrc :=\n    log.trace.filterMap\n      fun (src', str') => if src' == src then some str' else none\n  #check filterSrc\n  -- Logger.filterSrc {Src : Type} (log : Logger Src) (src : Src) [inst✝ : BEq Src] : List String\n\n  def lenSrc :=\n    log.filterSrc src |>.length\n  #check lenSrc\n  -- Logger.lenSrc {Src : Type} (log : Logger Src) (src : Src) [inst✝ : BEq Src] : Nat\nend Logger\n```\n\nThe following example demonstrates availability of variables in proofs:\n```lean\nvariable\n  {α : Type}    -- available in the proof as indirectly mentioned through `a`\n  [ToString α]  -- available in the proof as `α` is included\n  (a : α)       -- available in the proof as mentioned in the header\n  {β : Type}    -- not available in the proof\n  [ToString β]  -- not available in the proof\n\ntheorem ex : a = a := rfl\n```\nAfter elaboration of the proof, the following warning will be generated to highlight the unused\nhypothesis:\n```\nincluded section variable '[ToString α]' is not used in 'ex', consider excluding it\n```\nIn such cases, the offending variable declaration should be moved down or into a section so that\nonly theorems that do depend on it follow it until the end of the section.\n"}},
    "content": "variable"},
   "25":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Maybe.{u} (α : Type u) : Type u",
      "name": ["LeanW26", "Monads", "Maybe"],
      "isDef": true,
      "docs": null}},
    "content": "Maybe"},
   "247":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-1805",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "240":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-1776",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "237":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first'.{u} {α : Type u} (L : List α) : Oops α",
      "name": ["LeanW26", "Monads", "first'"],
      "isDef": false,
      "docs": null}},
    "content": "first'"},
   "233":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-1748",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "23":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.inductive-751",
      "name": ["Lean", "Parser", "Command", "inductive"],
      "docs":
      "In Lean, every concrete type other than the universes\nand every type constructor other than dependent arrows\nis an instance of a general family of type constructions known as inductive types.\nIt is remarkable that it is possible to construct a substantial edifice of mathematics\nbased on nothing more than the type universes, dependent arrow types, and inductive types;\neverything else follows from those.\nIntuitively, an inductive type is built up from a specified list of constructors.\nFor example, `List α` is the list of elements of type `α`, and is defined as follows:\n```\ninductive List (α : Type u) where\n| nil\n| cons (head : α) (tail : List α)\n```\nA list of elements of type `α` is either the empty list, `nil`,\nor an element `head : α` followed by a list `tail : List α`.\nSee [Inductive types](https://lean-lang.org/theorem_proving_in_lean4/inductive_types.html)\nfor more information.\n"}},
    "content": "inductive"},
   "228":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Oops.ok.{u} {α : Type u} : α → Oops α",
      "name": ["LeanW26", "Monads", "Oops", "ok"],
      "isDef": false,
      "docs": null}},
    "content": "ok"},
   "226":
   {"kind": {"var": {"type": "α", "name": "_uniq.22741"}}, "content": "x"},
   "224":
   {"kind":
    {"str": {"string": "Tried to get the first value of an empty list"}},
    "content": "\"Tried to get the first value of an empty list\""},
   "222":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.Oops.except.{u} {α : Type u} : String → Oops α",
      "name": ["LeanW26", "Monads", "Oops", "except"],
      "isDef": false,
      "docs": null}},
    "content": "except"},
   "220":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-1652",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "with"},
   "218":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Term.match-1652",
      "name": ["Lean", "Parser", "Term", "match"],
      "docs":
      "Pattern matching. `match e, ... with | p, ... => f | ...` matches each given\nterm `e` against each pattern `p` of a match alternative. When all patterns\nof an alternative match, the `match` term evaluates to the value of the\ncorresponding right-hand side `f` with the pattern variables bound to the\nrespective matched values.\nIf used as `match h : e, ... with | p, ... => f | ...`, `h : e = p` is available\nwithin `f`.\n\nWhen not constructing a proof, `match` does not automatically substitute variables\nmatched on in dependent variables' types. Use `match (generalizing := true) ...` to\nenforce this.\n\nSyntax quotations can also be used in a pattern match.\nThis matches a `Syntax` value against quotations, pattern variables, or `_`.\n\nQuoted identifiers only match identical identifiers - custom matching such as by the preresolved\nnames only should be done explicitly.\n\n`Syntax.atom`s are ignored during matching by default except when part of a built-in literal.\nFor users introducing new atoms, we recommend wrapping them in dedicated syntax kinds if they\nshould participate in matching.\nFor example, in\n```lean\nsyntax \"c\" (\"foo\" <|> \"bar\") ...\n```\n`foo` and `bar` are indistinguishable during matching, but in\n```lean\nsyntax foo := \"foo\"\nsyntax \"c\" (foo <|> \"bar\") ...\n```\nthey are not.\n"}},
    "content": "match"},
   "216":
   {"kind": {"var": {"type": "List α", "name": "_uniq.22705"}}, "content": "L"},
   "214":
   {"kind": {"var": {"type": "Type u", "name": "_uniq.22702"}}, "content": "α"},
   "212":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first'.{u} {α : Type u} (L : List α) : Oops α",
      "name": ["LeanW26", "Monads", "first'"],
      "isDef": true,
      "docs": null}},
    "content": "first'"},
   "210":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-1600",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "207": {"kind": "unknown", "content": "Oops"},
   "205":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.open-1589",
      "name": ["Lean", "Parser", "Command", "open"],
      "docs":
      "Makes names from other namespaces visible without writing the namespace prefix.\n\nNames that are made available with `open` are visible within the current `section` or `namespace`\nblock. This makes referring to (type) definitions and theorems easier, but note that it can also\nmake [scoped instances], notations, and attributes from a different namespace available.\n\nThe `open` command can be used in a few different ways:\n\n* `open Some.Namespace.Path1 Some.Namespace.Path2` makes all non-protected names in\n  `Some.Namespace.Path1` and `Some.Namespace.Path2` available without the prefix, so that\n  `Some.Namespace.Path1.x` and `Some.Namespace.Path2.y` can be referred to by writing only `x` and\n  `y`.\n\n* `open Some.Namespace.Path hiding def1 def2` opens all non-protected names in `Some.Namespace.Path`\n  except `def1` and `def2`.\n\n* `open Some.Namespace.Path (def1 def2)` only makes `Some.Namespace.Path.def1` and\n  `Some.Namespace.Path.def2` available without the full prefix, so `Some.Namespace.Path.def3` would\n  be unaffected.\n\n  This works even if `def1` and `def2` are `protected`.\n\n* `open Some.Namespace.Path renaming def1 → def1', def2 → def2'` same as `open Some.Namespace.Path\n  (def1 def2)` but `def1`/`def2`'s names are changed to `def1'`/`def2'`.\n\n  This works even if `def1` and `def2` are `protected`.\n\n* `open scoped Some.Namespace.Path1 Some.Namespace.Path2` **only** opens [scoped instances],\n  notations, and attributes from `Namespace1` and `Namespace2`; it does **not** make any other name\n  available.\n\n* `open <any of the open shapes above> in` makes the names `open`-ed visible only in the next\n  command or expression.\n\n[scoped instance]: https://lean-lang.org/theorem_proving_in_lean4/type_classes.html#scoped-instances\n(Scoped instances in Theorem Proving in Lean)\n\n\n## Examples\n\n```lean\n/-- SKI combinators https://en.wikipedia.org/wiki/SKI_combinator_calculus -/\nnamespace Combinator.Calculus\n  def I (a : α) : α := a\n  def K (a : α) : β → α := fun _ => a\n  def S (x : α → β → γ) (y : α → β) (z : α) : γ := x z (y z)\nend Combinator.Calculus\n\nsection\n  -- open everything under `Combinator.Calculus`, *i.e.* `I`, `K` and `S`,\n  -- until the section ends\n  open Combinator.Calculus\n\n  theorem SKx_eq_K : S K x = I := rfl\nend\n\n-- open everything under `Combinator.Calculus` only for the next command (the next `theorem`, here)\nopen Combinator.Calculus in\ntheorem SKx_eq_K' : S K x = I := rfl\n\nsection\n  -- open only `S` and `K` under `Combinator.Calculus`\n  open Combinator.Calculus (S K)\n\n  theorem SKxy_eq_y : S K x y = y := rfl\n\n  -- `I` is not in scope, we have to use its full path\n  theorem SKxy_eq_Iy : S K x y = Combinator.Calculus.I y := rfl\nend\n\nsection\n  open Combinator.Calculus\n    renaming\n      I → identity,\n      K → konstant\n\n  #check identity\n  #check konstant\nend\n\nsection\n  open Combinator.Calculus\n    hiding S\n\n  #check I\n  #check K\nend\n\nsection\n  namespace Demo\n    inductive MyType\n    | val\n\n    namespace N1\n      scoped infix:68 \" ≋ \" => BEq.beq\n\n      scoped instance : BEq MyType where\n        beq _ _ := true\n\n      def Alias := MyType\n    end N1\n  end Demo\n\n  -- bring `≋` and the instance in scope, but not `Alias`\n  open scoped Demo.N1\n\n  #check Demo.MyType.val == Demo.MyType.val\n  #check Demo.MyType.val ≋ Demo.MyType.val\n  -- #check Alias -- unknown identifier 'Alias'\nend\n```\n"}},
    "content": "open"},
   "202":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Oops.ok.{u} {α : Type u} : α → Oops α",
      "name": ["LeanW26", "Monads", "Oops", "ok"],
      "isDef": true,
      "docs": null}},
    "content": "ok"},
   "200":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Oops.{u} (α : Type u) : Type u",
      "name": ["LeanW26", "Monads", "Oops"],
      "isDef": false,
      "docs": null}},
    "content": "Oops"},
   "198":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.Oops.except.{u} {α : Type u} : String → Oops α",
      "name": ["LeanW26", "Monads", "Oops", "except"],
      "isDef": true,
      "docs": null}},
    "content": "except"},
   "196":
   {"kind":
    {"keyword": {"occurrence": "null-1526", "name": ["null"], "docs": null}},
    "content": "where"},
   "194":
   {"kind": {"var": {"type": "Type u", "name": "_uniq.21731"}}, "content": "α"},
   "192":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.Oops.{u} (α : Type u) : Type u",
      "name": ["LeanW26", "Monads", "Oops"],
      "isDef": true,
      "docs": null}},
    "content": "Oops"},
   "190":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.inductive-1497",
      "name": ["Lean", "Parser", "Command", "inductive"],
      "docs":
      "In Lean, every concrete type other than the universes\nand every type constructor other than dependent arrows\nis an instance of a general family of type constructions known as inductive types.\nIt is remarkable that it is possible to construct a substantial edifice of mathematics\nbased on nothing more than the type universes, dependent arrow types, and inductive types;\neverything else follows from those.\nIntuitively, an inductive type is built up from a specified list of constructors.\nFor example, `List α` is the list of elements of type `α`, and is defined as follows:\n```\ninductive List (α : Type u) where\n| nil\n| cons (head : α) (tail : List α)\n```\nA list of elements of type `α` is either the empty list, `nil`,\nor an element `head : α` followed by a list `tail : List α`.\nSee [Inductive types](https://lean-lang.org/theorem_proving_in_lean4/inductive_types.html)\nfor more information.\n"}},
    "content": "inductive"},
   "19": {"kind": "unknown", "content": "w"},
   "186":
   {"kind": {"str": {"string": "Romeo, Romeo, wherefore art thou Romeo?"}},
    "content": "\"Romeo, Romeo, wherefore art thou Romeo?\""},
   "184":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.first_char (s : String) : Maybe Char",
      "name": ["LeanW26", "Monads", "first_char"],
      "isDef": false,
      "docs": null}},
    "content": "first_char"},
   "180":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-1278",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "175":
   {"kind":
    {"keyword":
     {"occurrence": "termDepIfThenElse-1227",
      "name": ["termDepIfThenElse"],
      "docs":
      "\"Dependent\" if-then-else, normally written via the notation `if h : c then t(h) else e(h)`,\nis sugar for `dite c (fun h => t(h)) (fun h => e(h))`, and it is the same as\n`if c then t else e` except that `t` is allowed to depend on a proof `h : c`,\nand `e` can depend on `h : ¬c`. (Both branches use the same name for the hypothesis,\neven though it has different types in the two cases.)\n\nWe use this to be able to communicate the if-then-else condition to the branches.\nFor example, `Array.get arr i h` expects a proof `h : i < arr.size` in order to\navoid a bounds check, so you can write `if h : i < arr.size then arr.get i h else ...`\nto avoid the bounds check inside the if branch. (Of course in this case we have only\nlifted the check into an explicit `if`, but we could also use this proof multiple times\nor derive `i < arr.size` from some other proposition that we are checking in the `if`.)\n"}},
    "content": "else"},
   "171":
   {"kind":
    {"const":
     {"signature": "String.data (b : String) : List Char",
      "name": ["String", "data"],
      "isDef": false,
      "docs":
      "Converts a string to a list of characters.\n\nSince strings are represented as dynamic arrays of bytes containing the string encoded using\nUTF-8, this operation takes time and space linear in the length of the string.\n\nExamples:\n * `\"abc\".toList = ['a', 'b', 'c']`\n * `\"\".toList = []`\n * `\"\\n\".toList = ['\\n']`\n"}},
    "content": "data"},
   "17": {"kind": "unknown", "content": "v"},
   "164":
   {"kind":
    {"const":
     {"signature": "String.toList (s : String) : List Char",
      "name": ["String", "toList"],
      "isDef": false,
      "docs":
      "Converts a string to a list of characters.\n\nSince strings are represented as dynamic arrays of bytes containing the string encoded using\nUTF-8, this operation takes time and space linear in the length of the string.\n\nExamples:\n * `\"abc\".toList = ['a', 'b', 'c']`\n * `\"\".toList = []`\n * `\"\\n\".toList = ['\\n']`\n"}},
    "content": "String.toList"},
   "158":
   {"kind":
    {"const":
     {"signature": "String.data (b : String) : List Char",
      "name": ["String", "data"],
      "isDef": false,
      "docs":
      "Converts a string to a list of characters.\n\nSince strings are represented as dynamic arrays of bytes containing the string encoded using\nUTF-8, this operation takes time and space linear in the length of the string.\n\nExamples:\n * `\"abc\".toList = ['a', 'b', 'c']`\n * `\"\".toList = []`\n * `\"\\n\".toList = ['\\n']`\n"}},
    "content": "String.data"},
   "154":
   {"kind":
    {"keyword":
     {"occurrence": "termDepIfThenElse-1227",
      "name": ["termDepIfThenElse"],
      "docs":
      "\"Dependent\" if-then-else, normally written via the notation `if h : c then t(h) else e(h)`,\nis sugar for `dite c (fun h => t(h)) (fun h => e(h))`, and it is the same as\n`if c then t else e` except that `t` is allowed to depend on a proof `h : c`,\nand `e` can depend on `h : ¬c`. (Both branches use the same name for the hypothesis,\neven though it has different types in the two cases.)\n\nWe use this to be able to communicate the if-then-else condition to the branches.\nFor example, `Array.get arr i h` expects a proof `h : i < arr.size` in order to\navoid a bounds check, so you can write `if h : i < arr.size then arr.get i h else ...`\nto avoid the bounds check inside the if branch. (Of course in this case we have only\nlifted the check into an explicit `if`, but we could also use this proof multiple times\nor derive `i < arr.size` from some other proposition that we are checking in the `if`.)\n"}},
    "content": "then"},
   "152": {"kind": {"withType": {"type": "ℕ"}}, "content": "0"},
   "150": {"kind": "unknown", "content": ">"},
   "15": {"kind": "unknown", "content": "u"},
   "148":
   {"kind":
    {"const":
     {"signature": "String.length (b : String) : ℕ",
      "name": ["String", "length"],
      "isDef": false,
      "docs":
      "Returns the length of a string in Unicode code points.\n\nExamples:\n* `\"\".length = 0`\n* `\"abc\".length = 3`\n* `\"L∃∀N\".length = 4`\n"}},
    "content": "length"},
   "146": {"kind": "unknown", "content": "."},
   "144":
   {"kind": {"var": {"type": "s.length > 0", "name": "_uniq.15595"}},
    "content": "h"},
   "142":
   {"kind":
    {"keyword":
     {"occurrence": "termDepIfThenElse-1227",
      "name": ["termDepIfThenElse"],
      "docs":
      "\"Dependent\" if-then-else, normally written via the notation `if h : c then t(h) else e(h)`,\nis sugar for `dite c (fun h => t(h)) (fun h => e(h))`, and it is the same as\n`if c then t else e` except that `t` is allowed to depend on a proof `h : c`,\nand `e` can depend on `h : ¬c`. (Both branches use the same name for the hypothesis,\neven though it has different types in the two cases.)\n\nWe use this to be able to communicate the if-then-else condition to the branches.\nFor example, `Array.get arr i h` expects a proof `h : i < arr.size` in order to\navoid a bounds check, so you can write `if h : i < arr.size then arr.get i h else ...`\nto avoid the bounds check inside the if branch. (Of course in this case we have only\nlifted the check into an explicit `if`, but we could also use this proof multiple times\nor derive `i < arr.size` from some other proposition that we are checking in the `if`.)\n"}},
    "content": "if"},
   "140":
   {"kind":
    {"const":
     {"signature": "Char : Type",
      "name": ["Char"],
      "isDef": false,
      "docs":
      "Characters are Unicode [scalar values](http://www.unicode.org/glossary/#unicode_scalar_value).\n"}},
    "content": "Char"},
   "138":
   {"kind":
    {"const":
     {"signature": "String : Type",
      "name": ["String"],
      "isDef": false,
      "docs":
      "A string is a sequence of Unicode scalar values.\n\nAt runtime, strings are represented by [dynamic arrays](https://en.wikipedia.org/wiki/Dynamic_array)\nof bytes using the UTF-8 encoding. Both the size in bytes (`String.utf8ByteSize`) and in characters\n(`String.length`) are cached and take constant time. Many operations on strings perform in-place\nmodifications when the reference to the string is unique.\n"}},
    "content": "String"},
   "136":
   {"kind": {"var": {"type": "String", "name": "_uniq.15542"}}, "content": "s"},
   "134":
   {"kind":
    {"const":
     {"signature": "LeanW26.Monads.first_char (s : String) : Maybe Char",
      "name": ["LeanW26", "Monads", "first_char"],
      "isDef": true,
      "docs": null}},
    "content": "first_char"},
   "132":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.definition-1181",
      "name": ["Lean", "Parser", "Command", "definition"],
      "docs": null}},
    "content": "def"},
   "13":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.universe-42",
      "name": ["Lean", "Parser", "Command", "universe"],
      "docs":
      "Declares one or more universe variables.\n\n`universe u v`\n\n`Prop`, `Type`, `Type u` and `Sort u` are types that classify other types, also known as\n*universes*. In `Type u` and `Sort u`, the variable `u` stands for the universe's *level*, and a\nuniverse at level `u` can only classify universes that are at levels lower than `u`. For more\ndetails on type universes, please refer to [the relevant chapter of Theorem Proving in Lean][tpil\nuniverses].\n\nJust as type arguments allow polymorphic definitions to be used at many different types, universe\nparameters, represented by universe variables, allow a definition to be used at any required level.\nWhile Lean mostly handles universe levels automatically, declaring them explicitly can provide more\ncontrol when writing signatures. The `universe` keyword allows the declared universe variables to be\nused in a collection of definitions, and Lean will ensure that these definitions use them\nconsistently.\n\n[tpil universes]: https://lean-lang.org/theorem_proving_in_lean4/dependent_type_theory.html#types-as-objects\n(Type universes on Theorem Proving in Lean)\n\n```lean\n/- Explicit type-universe parameter. -/\ndef id₁.{u} (α : Type u) (a : α) := a\n\n/- Implicit type-universe parameter, equivalent to `id₁`.\n  Requires option `autoImplicit true`, which is the default. -/\ndef id₂ (α : Type u) (a : α) := a\n\n/- Explicit standalone universe variable declaration, equivalent to `id₁` and `id₂`. -/\nuniverse u\ndef id₃ (α : Type u) (a : α) := a\n```\n\nOn a more technical note, using a universe variable only in the right-hand side of a definition\ncauses an error if the universe has not been declared previously.\n\n```lean\ndef L₁.{u} := List (Type u)\n\n-- def L₂ := List (Type u) -- error: `unknown universe level 'u'`\n\nuniverse u\ndef L₃ := List (Type u)\n```\n\n## Examples\n\n```lean\nuniverse u v w\n\nstructure Pair (α : Type u) (β : Type v) : Type (max u v) where\n  a : α\n  b : β\n\n#check Pair.{v, w}\n-- Pair : Type v → Type w → Type (max v w)\n```\n"}},
    "content": "universe"},
   "128": {"kind": {"withType": {"type": "ℕ"}}, "content": "3"},
   "126": {"kind": {"withType": {"type": "ℕ"}}, "content": "2"},
   "124": {"kind": "unknown", "content": ","},
   "122": {"kind": {"withType": {"type": "ℕ"}}, "content": "1"},
   "118":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-1074",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "111":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Command.eval-1046",
      "name": ["Lean", "Parser", "Command", "eval"],
      "docs":
      "`#eval e` evaluates the expression `e` by compiling and evaluating it.\n\n* The command attempts to use `ToExpr`, `Repr`, or `ToString` instances to print the result.\n* If `e` is a monadic value of type `m ty`, then the command tries to adapt the monad `m`\n  to one of the monads that `#eval` supports, which include `IO`, `CoreM`, `MetaM`, `TermElabM`, and `CommandElabM`.\n  Users can define `MonadEval` instances to extend the list of supported monads.\n\nThe `#eval` command gracefully degrades in capability depending on what is imported.\nImporting the `Lean.Elab.Command` module provides full capabilities.\n\nDue to unsoundness, `#eval` refuses to evaluate expressions that depend on `sorry`, even indirectly,\nsince the presence of `sorry` can lead to runtime instability and crashes.\nThis check can be overridden with the `#eval! e` command.\n\nOptions:\n* If `eval.pp` is true (default: true) then tries to use `ToExpr` instances to make use of the\n  usual pretty printer. Otherwise, only tries using `Repr` and `ToString` instances.\n* If `eval.type` is true (default: false) then pretty prints the type of the evaluated value.\n* If `eval.derive.repr` is true (default: true) then attempts to auto-derive a `Repr` instance\n  when there is no other way to print the result.\n\nSee also: `#reduce e` for evaluation by term reduction.\n"}},
    "content": "#eval"},
   "107":
   {"kind":
    {"const":
     {"signature": "Nat : Type",
      "name": ["Nat"],
      "isDef": false,
      "docs":
      "The natural numbers, starting at zero.\n\nThis type is special-cased by both the kernel and the compiler, and overridden with an efficient\nimplementation. Both use a fast arbitrary-precision arithmetic library (usually\n[GMP](https://gmplib.org/)); at runtime, `Nat` values that are sufficiently small are unboxed.\n"}},
    "content": "Nat"},
   "105": {"kind": "unknown", "content": "α"},
   "103":
   {"kind":
    {"const":
     {"signature":
      "LeanW26.Monads.first.{u} {α : Type u} (L : List α) : Maybe α",
      "name": ["LeanW26", "Monads", "first"],
      "isDef": false,
      "docs": null}},
    "content": "first"},
   "10": {"kind": "unknown", "content": "LeanW26.Monads"},
   "0":
   {"kind":
    {"keyword":
     {"occurrence": "Lean.Parser.Module.import-0",
      "name": ["Lean", "Parser", "Module", "import"],
      "docs": null}},
    "content": "import"}},
  "nextKey": 684,
  "messageContents":
  {"98": {"append": [96, 97]},
   "97": {"text": "LeanW26.Monads.Maybe.none"},
   "96": {"text": ""},
   "678": {"append": [96, 677]},
   "677":
   {"text":
    "invalid 'import' command, it must be used in the beginning of the file"},
   "668": {"append": [96, 667]},
   "667": {"term": 666},
   "611": {"append": [96, 610]},
   "610": {"term": 609},
   "559": {"append": [96, 558]},
   "558":
   {"text":
    "Hello!\nThis is the IO Monad\nIf you run this code from the command line\nyou can use IO.getLine\nYou can examime the filesystem too.\nCurrent directory: /Users/ericklavins/Courses/LeanW26\n"},
   "513": {"append": [96, 512]},
   "512": {"term": 511},
   "502": {"append": [96, 501]},
   "501": {"text": "Except.ok 2"},
   "490": {"append": [96, 489]},
   "489": {"term": 488},
   "345": {"append": [96, 344]},
   "344": {"text": "LeanW26.Monads.Maybe.some [10, 20]"},
   "246": {"append": [96, 245]},
   "245": {"text": "LeanW26.Monads.Oops.ok 1"},
   "232": {"append": [96, 231]},
   "231":
   {"text":
    "LeanW26.Monads.Oops.except \"Tried to get the first value of an empty list\""},
   "179": {"append": [96, 178]},
   "178": {"text": "LeanW26.Monads.Maybe.some 'R'"},
   "170": {"append": [96, 169]},
   "169": {"append": [156, 161, 162, 163, 167, 168]},
   "168": {"text": "` instead"},
   "167": {"term": 166},
   "163": {"text": ": Use `"},
   "162": {"text": "` has been deprecated"},
   "161": {"term": 160},
   "156": {"text": "`"},
   "117": {"append": [96, 116]},
   "116": {"text": "LeanW26.Monads.Maybe.some 1"}},
  "goals": {},
  "code":
  {"95":
   {"seq":
    {"highlights":
     [58,
      60,
      2,
      62,
      2,
      64,
      66,
      2,
      32,
      2,
      34,
      2,
      36,
      68,
      2,
      28,
      70,
      2,
      32,
      2,
      72,
      2,
      66,
      38,
      2,
      32,
      2,
      47,
      2,
      66,
      2,
      74,
      2,
      76,
      2,
      70,
      2,
      78,
      41,
      43,
      2,
      80,
      82,
      2,
      84,
      2,
      86,
      41,
      43,
      2,
      88,
      90,
      92,
      2,
      84,
      2,
      94,
      2,
      88,
      5]}},
   "94": {"token": {"tok": 93}},
   "92": {"token": {"tok": 91}},
   "90": {"token": {"tok": 89}},
   "9": {"token": {"tok": 8}},
   "88": {"token": {"tok": 87}},
   "86": {"token": {"tok": 85}},
   "84": {"token": {"tok": 83}},
   "82": {"token": {"tok": 81}},
   "80": {"token": {"tok": 79}},
   "78": {"token": {"tok": 77}},
   "76": {"token": {"tok": 75}},
   "74": {"token": {"tok": 73}},
   "72": {"token": {"tok": 71}},
   "70": {"token": {"tok": 69}},
   "7": {"text": {"str": "\n\n"}},
   "683": {"seq": {"highlights": [676, 682, 2, 5]}},
   "682": {"span": {"info": [["error", 678]], "content": 681}},
   "681": {"seq": {"highlights": [680, 5]}},
   "680": {"token": {"tok": 679}},
   "68": {"token": {"tok": 67}},
   "676": {"text": {"str": "\n\n\n\n"}},
   "675": {"seq": {"highlights": [7, 672, 2, 674, 5]}},
   "674": {"token": {"tok": 673}},
   "672": {"span": {"info": [["info", 668]], "content": 671}},
   "671": {"seq": {"highlights": [670, 5]}},
   "670": {"token": {"tok": 669}},
   "666":
   {"seq":
    {"highlights":
     [157,
      590,
      157,
      643,
      602,
      2,
      645,
      602,
      2,
      647,
      602,
      2,
      649,
      602,
      2,
      651,
      602,
      2,
      653,
      602,
      2,
      655,
      602,
      2,
      657,
      602,
      2,
      659,
      602,
      2,
      661,
      602,
      2,
      663,
      602,
      2,
      665,
      608]}},
   "665": {"token": {"tok": 664}},
   "663": {"token": {"tok": 662}},
   "661": {"token": {"tok": 660}},
   "66": {"token": {"tok": 65}},
   "659": {"token": {"tok": 658}},
   "657": {"token": {"tok": 656}},
   "655": {"token": {"tok": 654}},
   "653": {"token": {"tok": 652}},
   "651": {"token": {"tok": 650}},
   "649": {"token": {"tok": 648}},
   "647": {"token": {"tok": 646}},
   "645": {"token": {"tok": 644}},
   "643": {"token": {"tok": 642}},
   "641":
   {"seq":
    {"highlights":
     [7,
      620,
      2,
      622,
      2,
      32,
      2,
      72,
      2,
      108,
      2,
      74,
      2,
      624,
      41,
      626,
      2,
      628,
      2,
      445,
      2,
      80,
      123,
      125,
      2,
      127,
      82,
      41,
      630,
      2,
      632,
      2,
      445,
      2,
      80,
      129,
      125,
      585,
      125,
      587,
      82,
      41,
      634,
      2,
      636,
      2,
      445,
      2,
      80,
      638,
      125,
      640,
      82,
      41,
      482,
      2,
      28,
      628,
      480,
      632,
      480,
      636,
      38,
      5]}},
   "640": {"token": {"tok": 639}},
   "64": {"token": {"tok": 63}},
   "638": {"token": {"tok": 637}},
   "636": {"token": {"tok": 635}},
   "634": {"token": {"tok": 633}},
   "632": {"token": {"tok": 631}},
   "630": {"token": {"tok": 629}},
   "628": {"token": {"tok": 627}},
   "626": {"token": {"tok": 625}},
   "624": {"token": {"tok": 623}},
   "622": {"token": {"tok": 621}},
   "620": {"token": {"tok": 619}},
   "62": {"token": {"tok": 61}},
   "618": {"seq": {"highlights": [7, 615, 2, 617, 5]}},
   "617": {"token": {"tok": 616}},
   "615": {"span": {"info": [["info", 611]], "content": 614}},
   "614": {"seq": {"highlights": [613, 5]}},
   "613": {"token": {"tok": 612}},
   "609":
   {"seq":
    {"highlights":
     [157,
      590,
      157,
      592,
      594,
      596,
      2,
      598,
      600,
      602,
      2,
      592,
      594,
      596,
      2,
      604,
      600,
      602,
      2,
      592,
      594,
      596,
      2,
      606,
      600,
      602,
      2,
      592,
      487,
      596,
      2,
      598,
      600,
      602,
      2,
      592,
      487,
      596,
      2,
      604,
      600,
      602,
      2,
      592,
      487,
      596,
      2,
      606,
      600,
      608]}},
   "608": {"token": {"tok": 607}},
   "606": {"token": {"tok": 605}},
   "604": {"token": {"tok": 603}},
   "602": {"token": {"tok": 601}},
   "600": {"token": {"tok": 599}},
   "60": {"token": {"tok": 59}},
   "6": {"seq": {"highlights": [1, 2, 4, 5]}},
   "598": {"token": {"tok": 597}},
   "596": {"token": {"tok": 595}},
   "594": {"token": {"tok": 593}},
   "592": {"token": {"tok": 591}},
   "590": {"token": {"tok": 589}},
   "588":
   {"seq":
    {"highlights":
     [567,
      569,
      2,
      571,
      2,
      32,
      2,
      72,
      2,
      28,
      108,
      2,
      573,
      2,
      108,
      38,
      2,
      74,
      2,
      575,
      41,
      577,
      2,
      579,
      2,
      445,
      2,
      80,
      123,
      125,
      2,
      127,
      82,
      41,
      581,
      2,
      583,
      2,
      445,
      2,
      80,
      129,
      125,
      585,
      125,
      587,
      82,
      41,
      482,
      2,
      28,
      579,
      125,
      2,
      583,
      38,
      5]}},
   "587": {"token": {"tok": 586}},
   "585": {"token": {"tok": 584}},
   "583": {"token": {"tok": 582}},
   "581": {"token": {"tok": 580}},
   "58":
   {"text":
    {"str":
     "\n\n/- For example, you might ask for the first value of an empty list. -/\n\n"}},
   "579": {"token": {"tok": 578}},
   "577": {"token": {"tok": 576}},
   "575": {"token": {"tok": 574}},
   "573": {"token": {"tok": 572}},
   "571": {"token": {"tok": 570}},
   "57": {"seq": {"highlights": [7, 54, 2, 56, 5]}},
   "569": {"token": {"tok": 568}},
   "567":
   {"text":
    {"str":
     "\n\n/-\nThe List Monad\n===\nMathlib adds a Monad instance for lists that allows for nondeterminism.\nIt is defined something like this\n\n```lean\ninstance : Monad List where\n  pure x := [x]\n  bind xs f := xs.foldr (fun a acc => f a ++ acc) []\n```\n\n-/\n\n"}},
   "566": {"seq": {"highlights": [7, 563, 2, 565, 5]}},
   "565": {"token": {"tok": 564}},
   "563": {"span": {"info": [["info", 559]], "content": 562}},
   "562": {"seq": {"highlights": [561, 5]}},
   "561": {"token": {"tok": 560}},
   "56": {"token": {"tok": 55}},
   "557":
   {"seq":
    {"highlights":
     [522,
      524,
      2,
      526,
      2,
      32,
      2,
      528,
      2,
      530,
      2,
      74,
      2,
      532,
      41,
      534,
      2,
      536,
      41,
      534,
      2,
      538,
      41,
      534,
      2,
      540,
      41,
      534,
      2,
      542,
      41,
      534,
      2,
      544,
      41,
      546,
      2,
      548,
      2,
      445,
      2,
      550,
      41,
      534,
      2,
      552,
      554,
      548,
      556,
      5]}},
   "556": {"token": {"tok": 555}},
   "554": {"token": {"tok": 553}},
   "552": {"token": {"tok": 551}},
   "550": {"token": {"tok": 549}},
   "548": {"token": {"tok": 547}},
   "546": {"token": {"tok": 545}},
   "544": {"token": {"tok": 543}},
   "542": {"token": {"tok": 541}},
   "540": {"token": {"tok": 539}},
   "54": {"token": {"tok": 53}},
   "538": {"token": {"tok": 537}},
   "536": {"token": {"tok": 535}},
   "534": {"token": {"tok": 533}},
   "532": {"token": {"tok": 531}},
   "530": {"token": {"tok": 529}},
   "528": {"token": {"tok": 527}},
   "526": {"token": {"tok": 525}},
   "524": {"token": {"tok": 523}},
   "522":
   {"text":
    {"str":
     "\n\n/- Many more: `Reader`, `StateM`, `IO`, `RandomM`. -/\n\n/-\nThe IO Monad\n===\n-/\n\n"}},
   "521": {"seq": {"highlights": [110, 517, 2, 496, 2, 519, 2, 123, 520, 5]}},
   "520": {"text": {"str": "                   -- 2"}},
   "52":
   {"seq":
    {"highlights":
     [22,
      24,
      2,
      26,
      2,
      28,
      30,
      2,
      32,
      2,
      34,
      2,
      36,
      38,
      2,
      40,
      41,
      43,
      2,
      45,
      2,
      32,
      2,
      47,
      2,
      30,
      41,
      43,
      2,
      49,
      2,
      32,
      2,
      30,
      2,
      51,
      2,
      47,
      2,
      30,
      5]}},
   "519": {"token": {"tok": 518}},
   "517": {"span": {"info": [["info", 513]], "content": 516}},
   "516": {"seq": {"highlights": [515, 5]}},
   "515": {"token": {"tok": 514}},
   "511": {"seq": {"highlights": [157, 487, 157]}},
   "510":
   {"seq":
    {"highlights": [110, 506, 2, 496, 2, 28, 508, 2, 139, 38, 2, 123, 509, 5]}},
   "51": {"token": {"tok": 50}},
   "509": {"text": {"str": "      -- ok 2"}},
   "508": {"token": {"tok": 507}},
   "506": {"span": {"info": [["info", 502]], "content": 505}},
   "505": {"seq": {"highlights": [504, 5]}},
   "504": {"token": {"tok": 503}},
   "500": {"seq": {"highlights": [7, 494, 2, 496, 2, 498, 2, 123, 499, 5]}},
   "5": {"seq": {"highlights": []}},
   "499": {"text": {"str": "               -- some 2"}},
   "498": {"token": {"tok": 497}},
   "496": {"token": {"tok": 495}},
   "494": {"span": {"info": [["info", 490]], "content": 493}},
   "493": {"seq": {"highlights": [492, 5]}},
   "492": {"token": {"tok": 491}},
   "49": {"token": {"tok": 48}},
   "488": {"seq": {"highlights": [157, 485, 2, 487]}},
   "487": {"token": {"tok": 486}},
   "485": {"token": {"tok": 484}},
   "483":
   {"seq":
    {"highlights":
     [464,
      466,
      2,
      468,
      2,
      28,
      470,
      2,
      32,
      2,
      34,
      2,
      51,
      2,
      34,
      38,
      2,
      80,
      393,
      2,
      470,
      82,
      2,
      28,
      472,
      2,
      32,
      2,
      108,
      38,
      2,
      32,
      2,
      470,
      2,
      108,
      2,
      74,
      2,
      474,
      41,
      476,
      2,
      478,
      2,
      74,
      2,
      472,
      2,
      480,
      2,
      127,
      41,
      482,
      2,
      478,
      5]}},
   "482": {"token": {"tok": 481}},
   "480": {"token": {"tok": 479}},
   "478": {"token": {"tok": 477}},
   "476": {"token": {"tok": 475}},
   "474": {"token": {"tok": 473}},
   "472": {"token": {"tok": 471}},
   "470": {"token": {"tok": 469}},
   "47": {"token": {"tok": 46}},
   "468": {"token": {"tok": 467}},
   "466": {"token": {"tok": 465}},
   "464":
   {"text":
    {"str":
     "\n\n/-\nIt is tempting to think of this as procedural code. But\nit is not. It is just syntax for:\n\n```lean\ndef first_two (L : List α) : Maybe (List α) :=\n  andThen (first L) (fun x =>\n  andThen (first L.tail) (fun y =>\n  some [x,y]))\n```\n-/\n\n\n/-\nExercises\n===\n\n<ex/> Retwrite `add_firsts` using `first`, `do` and `let`.\n\n<ex/> Instantiave `Oops` as a monad.\n\n-/\n\n\n\n/-\nLean/Mathlib Monads\n===\n\n`Option` What we've been calling `Maybe`\n`Except` What we've been calling `Oops`.\n`Id` The identity monad. Just returns its value.\n\n-/\n\n"}},
   "463":
   {"seq":
    {"highlights": [110, 462, 2, 457, 2, 80, 342, 125, 351, 125, 358, 82, 5]}},
   "462": {"span": {"info": [["info", 345]], "content": 461}},
   "461": {"seq": {"highlights": [460, 5]}},
   "460": {"token": {"tok": 459}},
   "458": {"seq": {"highlights": [7, 455, 2, 457, 2, 80, 342, 82, 5]}},
   "457": {"token": {"tok": 456}},
   "455": {"span": {"info": [["info", 98]], "content": 454}},
   "454": {"seq": {"highlights": [453, 5]}},
   "453": {"token": {"tok": 452}},
   "451":
   {"seq":
    {"highlights":
     [431,
      433,
      2,
      435,
      2,
      28,
      437,
      2,
      32,
      2,
      72,
      2,
      258,
      38,
      2,
      32,
      2,
      47,
      2,
      28,
      72,
      2,
      258,
      38,
      2,
      74,
      2,
      439,
      41,
      441,
      2,
      443,
      2,
      445,
      2,
      104,
      2,
      437,
      446,
      41,
      448,
      2,
      450,
      2,
      445,
      2,
      104,
      2,
      437,
      147,
      324,
      41,
      94,
      2,
      80,
      443,
      125,
      450,
      82,
      5]}},
   "450": {"token": {"tok": 449}},
   "45": {"token": {"tok": 44}},
   "448": {"token": {"tok": 447}},
   "446": {"text": {"str": "               -- This is different than let :="}},
   "445": {"token": {"tok": 444}},
   "443": {"token": {"tok": 442}},
   "441": {"token": {"tok": 440}},
   "439": {"token": {"tok": 438}},
   "437": {"token": {"tok": 436}},
   "435": {"token": {"tok": 434}},
   "433": {"token": {"tok": 432}},
   "431":
   {"text":
    {"str":
     "\n\n/-\nDo and let\n===\nYou can also use Lean's `do`, which makes the example even\nmore concise.\n-/\n\n"}},
   "430":
   {"seq":
    {"highlights":
     [413,
      415,
      2,
      417,
      2,
      28,
      419,
      2,
      32,
      2,
      72,
      2,
      258,
      38,
      2,
      32,
      2,
      47,
      2,
      28,
      72,
      2,
      258,
      38,
      2,
      74,
      41,
      104,
      2,
      419,
      377,
      421,
      2,
      423,
      2,
      425,
      2,
      84,
      41,
      104,
      2,
      419,
      147,
      324,
      2,
      421,
      2,
      427,
      2,
      429,
      2,
      84,
      41,
      94,
      2,
      80,
      425,
      125,
      429,
      82,
      5]}},
   "43": {"token": {"tok": 42}},
   "429": {"token": {"tok": 428}},
   "427": {"token": {"tok": 426}},
   "425": {"token": {"tok": 424}},
   "423": {"token": {"tok": 422}},
   "421": {"token": {"tok": 420}},
   "419": {"token": {"tok": 418}},
   "417": {"token": {"tok": 416}},
   "415": {"token": {"tok": 414}},
   "413":
   {"text":
    {"str":
     "\n\n/-\nLean's Monad Notation\n===\n\nOnce registered, you can use Lean's `>>=` syntax, which is like our `~~>` syntax.\n-/\n\n"}},
   "412":
   {"seq":
    {"highlights":
     [389,
      391,
      2,
      32,
      2,
      393,
      2,
      47,
      2,
      395,
      41,
      397,
      2,
      399,
      2,
      74,
      2,
      94,
      2,
      399,
      41,
      401,
      2,
      403,
      2,
      405,
      2,
      74,
      2,
      407,
      2,
      403,
      2,
      409,
      284,
      43,
      2,
      302,
      2,
      84,
      2,
      86,
      284,
      43,
      2,
      305,
      2,
      411,
      2,
      84,
      2,
      405,
      2,
      411,
      5]}},
   "411": {"token": {"tok": 410}},
   "41": {"text": {"str": "\n  "}},
   "409": {"token": {"tok": 408}},
   "407": {"token": {"tok": 406}},
   "405": {"token": {"tok": 404}},
   "403": {"token": {"tok": 402}},
   "401": {"token": {"tok": 400}},
   "40": {"token": {"tok": 39}},
   "4": {"token": {"tok": 3}},
   "399": {"token": {"tok": 398}},
   "397": {"token": {"tok": 396}},
   "395": {"token": {"tok": 394}},
   "393": {"token": {"tok": 392}},
   "391": {"token": {"tok": 390}},
   "389":
   {"text":
    {"str":
     "\n\n\n/-\nExercises\n===\n\n<ex/> Define a function `add_firsts` that returns the sum of the first two elements of a list\nof natural numbers, if they exist. Use `first_two`, chaining and `List.reverse`.\n\n<ex/> Make another version of this function, but instead of returning `Maybe (List Nat)`,\nreturn an `Oops (List Nat)`.\n\n-/\n\n\n\n/-\nRegistering Maybe as a Monad\n===\n\nOur types `Maybe` and `Oops` are not monads yet. We need to instantiate\nLean's `Monad` class first.\n\nLean's `Monad` class usually just needs two fields instantiated:\n\n- `pure` Takes a value and puts it into the monadic context.\nIt’s the way to lift a plain value into the monad without adding any effects.\n\n- `bind` Sequences computations. It takes a monadic value and a\nfunction that returns another monadic value, and combines them so that the second computation can depend on the result of the first.\n\nFor `Maybe` this works out to:\n\n-/\n\n"}},
   "388":
   {"seq":
    {"highlights":
     [370,
      372,
      2,
      374,
      2,
      28,
      376,
      2,
      32,
      2,
      72,
      2,
      258,
      38,
      2,
      32,
      2,
      47,
      2,
      28,
      72,
      2,
      258,
      38,
      2,
      74,
      41,
      104,
      2,
      376,
      377,
      379,
      2,
      381,
      2,
      383,
      2,
      84,
      41,
      104,
      2,
      376,
      147,
      324,
      2,
      379,
      2,
      385,
      2,
      387,
      2,
      84,
      41,
      94,
      2,
      80,
      383,
      125,
      387,
      82,
      5]}},
   "387": {"token": {"tok": 386}},
   "385": {"token": {"tok": 384}},
   "383": {"token": {"tok": 382}},
   "381": {"token": {"tok": 380}},
   "38": {"token": {"tok": 37}},
   "379": {"token": {"tok": 378}},
   "377": {"text": {"str": "      "}},
   "376": {"token": {"tok": 375}},
   "374": {"token": {"tok": 373}},
   "372": {"token": {"tok": 371}},
   "370":
   {"text":
    {"str":
     "\n\n/-\nNow we can see the structure of what we've built more clearly.\n-/\n"}},
   "369":
   {"seq": {"highlights": [362, 364, 32, 366, 2, 368, 2, 84, 2, 318, 5]}},
   "368": {"token": {"tok": 367}},
   "366": {"token": {"tok": 365}},
   "364": {"token": {"tok": 363}},
   "362":
   {"text":
    {"str":
     "\n\n/-\nAdding Syntax for Chaining\n===\n\nWe can define notation for chainging to make this even more clear:\n-/\n\n"}},
   "361":
   {"seq":
    {"highlights":
     [110, 356, 2, 335, 2, 80, 342, 125, 351, 125, 358, 125, 360, 82, 5]}},
   "360": {"token": {"tok": 359}},
   "36": {"token": {"tok": 35}},
   "358": {"token": {"tok": 357}},
   "356": {"span": {"info": [["info", 345]], "content": 355}},
   "355": {"seq": {"highlights": [354, 5]}},
   "354": {"token": {"tok": 353}},
   "352":
   {"seq": {"highlights": [110, 349, 2, 335, 2, 80, 342, 125, 351, 82, 5]}},
   "351": {"token": {"tok": 350}},
   "349": {"span": {"info": [["info", 345]], "content": 348}},
   "348": {"seq": {"highlights": [347, 5]}},
   "347": {"token": {"tok": 346}},
   "343": {"seq": {"highlights": [110, 340, 2, 335, 2, 80, 342, 82, 5]}},
   "342": {"token": {"tok": 341}},
   "340": {"span": {"info": [["info", 98]], "content": 339}},
   "34": {"token": {"tok": 33}},
   "339": {"seq": {"highlights": [338, 5]}},
   "338": {"token": {"tok": 337}},
   "336":
   {"seq":
    {"highlights": [7, 333, 2, 335, 2, 28, 80, 82, 32, 72, 2, 108, 38, 5]}},
   "335": {"token": {"tok": 334}},
   "333": {"span": {"info": [["info", 98]], "content": 332}},
   "332": {"seq": {"highlights": [331, 5]}},
   "331": {"token": {"tok": 330}},
   "329":
   {"seq":
    {"highlights":
     [310,
      312,
      2,
      314,
      2,
      28,
      316,
      2,
      32,
      2,
      72,
      2,
      258,
      38,
      2,
      32,
      2,
      47,
      2,
      28,
      72,
      2,
      258,
      38,
      2,
      74,
      41,
      318,
      2,
      28,
      104,
      2,
      316,
      38,
      2,
      28,
      320,
      2,
      322,
      2,
      84,
      41,
      318,
      2,
      28,
      104,
      2,
      316,
      147,
      324,
      38,
      2,
      28,
      326,
      2,
      328,
      2,
      84,
      41,
      94,
      2,
      80,
      322,
      125,
      328,
      82,
      38,
      38,
      5]}},
   "328": {"token": {"tok": 327}},
   "326": {"token": {"tok": 325}},
   "324": {"token": {"tok": 323}},
   "322": {"token": {"tok": 321}},
   "320": {"token": {"tok": 319}},
   "32": {"token": {"tok": 31}},
   "318": {"token": {"tok": 317}},
   "316": {"token": {"tok": 315}},
   "314": {"token": {"tok": 313}},
   "312": {"token": {"tok": 311}},
   "310": {"text": {"str": "\n\n/- Now we can write: -/\n\n"}},
   "309":
   {"seq":
    {"highlights":
     [288,
      290,
      2,
      292,
      2,
      28,
      294,
      2,
      32,
      2,
      47,
      2,
      258,
      38,
      2,
      28,
      296,
      2,
      32,
      2,
      258,
      2,
      51,
      2,
      47,
      2,
      262,
      38,
      2,
      32,
      2,
      47,
      2,
      262,
      2,
      74,
      41,
      298,
      2,
      294,
      2,
      300,
      41,
      43,
      2,
      302,
      2,
      84,
      2,
      86,
      303,
      41,
      43,
      2,
      305,
      2,
      307,
      2,
      84,
      2,
      296,
      2,
      307,
      308,
      5]}},
   "308": {"text": {"str": "     -- keep going"}},
   "307": {"token": {"tok": 306}},
   "305": {"token": {"tok": 304}},
   "303": {"text": {"str": "         -- stop here"}},
   "302": {"token": {"tok": 301}},
   "300": {"token": {"tok": 299}},
   "30": {"token": {"tok": 29}},
   "298": {"token": {"tok": 297}},
   "296": {"token": {"tok": 295}},
   "294": {"token": {"tok": 293}},
   "292": {"token": {"tok": 291}},
   "290": {"token": {"tok": 289}},
   "288":
   {"text":
    {"str":
     "\n\n/- But this does not really use the power of Monads. -/\n\n/-\nChaining Monads\n===\nAlternatively, you could define a chaining operator `andThen`: -/\n\n"}},
   "287":
   {"seq":
    {"highlights":
     [7,
      267,
      2,
      269,
      2,
      28,
      271,
      2,
      32,
      2,
      72,
      2,
      258,
      38,
      2,
      32,
      2,
      47,
      2,
      28,
      72,
      2,
      258,
      38,
      2,
      74,
      2,
      273,
      2,
      271,
      2,
      275,
      41,
      43,
      2,
      80,
      82,
      2,
      84,
      2,
      86,
      41,
      43,
      2,
      277,
      90,
      279,
      2,
      84,
      2,
      281,
      2,
      279,
      2,
      283,
      284,
      43,
      2,
      80,
      82,
      2,
      84,
      2,
      86,
      284,
      43,
      2,
      286,
      90,
      92,
      2,
      84,
      2,
      94,
      2,
      80,
      277,
      125,
      286,
      82,
      5]}},
   "286": {"token": {"tok": 285}},
   "284": {"text": {"str": "\n    "}},
   "283": {"token": {"tok": 282}},
   "281": {"token": {"tok": 280}},
   "28": {"token": {"tok": 27}},
   "279": {"token": {"tok": 278}},
   "277": {"token": {"tok": 276}},
   "275": {"token": {"tok": 274}},
   "273": {"token": {"tok": 272}},
   "271": {"token": {"tok": 270}},
   "269": {"token": {"tok": 268}},
   "267": {"token": {"tok": 266}},
   "265":
   {"seq":
    {"highlights":
     [252,
      254,
      2,
      64,
      256,
      2,
      32,
      2,
      34,
      2,
      36,
      68,
      2,
      64,
      258,
      2,
      32,
      2,
      34,
      2,
      260,
      68,
      2,
      64,
      262,
      2,
      32,
      2,
      34,
      2,
      264,
      68,
      5]}},
   "264": {"token": {"tok": 263}},
   "262": {"token": {"tok": 261}},
   "260": {"token": {"tok": 259}},
   "26": {"token": {"tok": 25}},
   "258": {"token": {"tok": 257}},
   "256": {"token": {"tok": 255}},
   "254": {"token": {"tok": 253}},
   "252":
   {"text":
    {"str":
     "\n\n\n\n/-\nNon Idomatic Use of Monads\n===\nSuppose you want to get the first two elements of a list. One thing you could\ndo is.\n-/\n\n"}},
   "251":
   {"seq":
    {"highlights": [110, 250, 2, 238, 2, 80, 123, 125, 127, 125, 129, 82, 5]}},
   "250": {"span": {"info": [["info", 246]], "content": 249}},
   "249": {"seq": {"highlights": [248, 5]}},
   "248": {"token": {"tok": 247}},
   "244":
   {"seq":
    {"highlights":
     [110, 243, 2, 238, 2, 28, 80, 82, 2, 32, 2, 72, 2, 108, 38, 5]}},
   "243": {"span": {"info": [["info", 232]], "content": 242}},
   "242": {"seq": {"highlights": [241, 5]}},
   "241": {"token": {"tok": 240}},
   "24": {"token": {"tok": 23}},
   "239":
   {"seq":
    {"highlights":
     [7, 236, 2, 238, 2, 80, 82, 2, 28, 106, 2, 74, 2, 108, 38, 5]}},
   "238": {"token": {"tok": 237}},
   "236": {"span": {"info": [["info", 232]], "content": 235}},
   "235": {"seq": {"highlights": [234, 5]}},
   "234": {"token": {"tok": 233}},
   "230":
   {"seq":
    {"highlights":
     [7,
      211,
      2,
      213,
      2,
      64,
      215,
      2,
      32,
      2,
      34,
      2,
      36,
      68,
      2,
      28,
      217,
      2,
      32,
      2,
      72,
      2,
      215,
      38,
      2,
      32,
      2,
      201,
      2,
      215,
      2,
      74,
      2,
      219,
      2,
      217,
      2,
      221,
      41,
      43,
      2,
      80,
      82,
      2,
      84,
      2,
      223,
      2,
      225,
      41,
      43,
      2,
      227,
      90,
      92,
      2,
      84,
      2,
      229,
      2,
      227,
      5]}},
   "229": {"token": {"tok": 228}},
   "227": {"token": {"tok": 226}},
   "225": {"token": {"tok": 224}},
   "223": {"token": {"tok": 222}},
   "221": {"token": {"tok": 220}},
   "22":
   {"text":
    {"str":
     "\n\n\n--notdone\n\n/-\nMonads\n===\n\nWhat's a Monad\n===\n\nMonads are\n- a data type that allows for sequencing, side effects, and off-path\ninformation.\n- a way to make functional programming look like procedural programming\n- supported by deep math, especially category theory\n\nLean implements Monads extensively in its metaprogramming framework.\n\nOther languages that use Monads are: Haskell, Agda, F#, OCaml.\nLanguages with Monad like libraries include: Scala, Rust, Javascript (promises),\n\n\n-/\n\n/-\nExample Monad : Maybe\n===\nIn Lean this monad is called `Option`. We'll rebuild it here and call it `Maybe` instead.\nThe idea is to provide a `none` value when the result of an operation is undefined.\n-/\n\n"}},
   "219": {"token": {"tok": 218}},
   "217": {"token": {"tok": 216}},
   "215": {"token": {"tok": 214}},
   "213": {"token": {"tok": 212}},
   "211": {"token": {"tok": 210}},
   "21": {"seq": {"highlights": [7, 14, 2, 16, 2, 18, 2, 20, 5]}},
   "209": {"seq": {"highlights": [7, 206, 2, 208, 5]}},
   "208": {"token": {"tok": 207}},
   "206": {"token": {"tok": 205}},
   "204":
   {"seq":
    {"highlights":
     [189,
      191,
      2,
      193,
      2,
      28,
      195,
      2,
      32,
      2,
      34,
      2,
      36,
      38,
      2,
      197,
      41,
      43,
      2,
      199,
      2,
      32,
      2,
      139,
      2,
      51,
      2,
      201,
      2,
      195,
      41,
      43,
      2,
      203,
      2,
      32,
      2,
      195,
      2,
      51,
      2,
      201,
      2,
      195,
      5]}},
   "203": {"token": {"tok": 202}},
   "201": {"token": {"tok": 200}},
   "20": {"token": {"tok": 19}},
   "2": {"text": {"str": " "}},
   "199": {"token": {"tok": 198}},
   "197": {"token": {"tok": 196}},
   "195": {"token": {"tok": 194}},
   "193": {"token": {"tok": 192}},
   "191": {"token": {"tok": 190}},
   "189":
   {"text":
    {"str":
     "\n\n\n/-\nExample Monod: Oops\n===\n\nLean does not have a built-in exception handler. But it does have\nan `Except` monad. We rebuild it here, but call it `Oops`.\n\n-/\n\n"}},
   "188": {"seq": {"highlights": [7, 183, 2, 185, 2, 187, 5]}},
   "187": {"token": {"tok": 186}},
   "185": {"token": {"tok": 184}},
   "183": {"span": {"info": [["info", 179]], "content": 182}},
   "182": {"seq": {"highlights": [181, 5]}},
   "181": {"token": {"tok": 180}},
   "18": {"token": {"tok": 17}},
   "177":
   {"seq":
    {"highlights":
     [131,
      133,
      2,
      135,
      2,
      28,
      137,
      2,
      32,
      2,
      139,
      38,
      2,
      32,
      2,
      47,
      2,
      141,
      2,
      74,
      41,
      143,
      2,
      145,
      2,
      32,
      2,
      137,
      147,
      149,
      2,
      151,
      2,
      153,
      2,
      155,
      2,
      94,
      2,
      137,
      147,
      174,
      80,
      153,
      82,
      2,
      176,
      2,
      86,
      5]}},
   "176": {"token": {"tok": 175}},
   "174": {"span": {"info": [["warning", 170]], "content": 173}},
   "173": {"seq": {"highlights": [172, 5]}},
   "172": {"token": {"tok": 171}},
   "166": {"seq": {"highlights": [157, 165, 157]}},
   "165": {"token": {"tok": 164}},
   "160": {"seq": {"highlights": [157, 159, 157]}},
   "16": {"token": {"tok": 15}},
   "159": {"token": {"tok": 158}},
   "157": {"text": {"str": ""}},
   "155": {"token": {"tok": 154}},
   "153": {"token": {"tok": 152}},
   "151": {"token": {"tok": 150}},
   "149": {"token": {"tok": 148}},
   "147": {"token": {"tok": 146}},
   "145": {"token": {"tok": 144}},
   "143": {"token": {"tok": 142}},
   "141": {"token": {"tok": 140}},
   "14": {"token": {"tok": 13}},
   "139": {"token": {"tok": 138}},
   "137": {"token": {"tok": 136}},
   "135": {"token": {"tok": 134}},
   "133": {"token": {"tok": 132}},
   "131":
   {"text":
    {"str":
     "\n\n/-\nMaybe is Polymorphic\n===\nOr you might ask for the first character in a string.\n-/\n\n"}},
   "130":
   {"seq":
    {"highlights": [110, 121, 2, 104, 2, 80, 123, 125, 127, 125, 129, 82, 5]}},
   "129": {"token": {"tok": 128}},
   "127": {"token": {"tok": 126}},
   "125": {"token": {"tok": 124}},
   "123": {"token": {"tok": 122}},
   "121": {"span": {"info": [["info", 117]], "content": 120}},
   "120": {"seq": {"highlights": [119, 5]}},
   "12": {"seq": {"highlights": [7, 9, 2, 11, 5]}},
   "119": {"token": {"tok": 118}},
   "115":
   {"seq":
    {"highlights":
     [110, 114, 2, 104, 2, 28, 80, 82, 2, 32, 2, 72, 2, 108, 38, 5]}},
   "114": {"span": {"info": [["info", 98]], "content": 113}},
   "113": {"seq": {"highlights": [112, 5]}},
   "112": {"token": {"tok": 111}},
   "110": {"text": {"str": "\n"}},
   "11": {"token": {"tok": 10}},
   "109":
   {"seq":
    {"highlights":
     [7, 102, 2, 104, 2, 80, 82, 2, 28, 106, 2, 74, 2, 108, 38, 5]}},
   "108": {"token": {"tok": 107}},
   "106": {"token": {"tok": 105}},
   "104": {"token": {"tok": 103}},
   "102": {"span": {"info": [["info", 98]], "content": 101}},
   "101": {"seq": {"highlights": [100, 5]}},
   "100": {"token": {"tok": 99}},
   "1": {"token": {"tok": 0}}}}}
